# Design: Extract the private Evidence coverage core (#776)

Generated by `/office-hours` on 2026-08-23
Branch: `main`
Repository: `forge-trust/AppSurface`
Status: APPROVED — private-core extraction selected and autoplan-reviewed
Mode: Builder
Supersedes: none; the nearest `main` design is the unrelated #771 Python-docstring spike.
Builds on: [contract-first EvidenceHost design](https://github.com/forge-trust/AppSurface/blob/main/docs/designs/appsurface-evidencehost-contract-first.md) and [#775](https://github.com/forge-trust/AppSurface/pull/775)

## Problem Statement

[#776](https://github.com/forge-trust/AppSurface/issues/776) must make the existing AppSurface coverage implementation available to the first-party Evidence path **without** reimplementing coverage behavior or starting a second `appsurface` process.

Today `CoverageRunWorkflow`, `CoverageGateEvaluator`, and their coupled filesystem, artifact, merge, watchdog, and patch-diff helpers live under `Cli/ForgeTrust.AppSurface.Cli`. The first Evidence command added in #775 reaches directly into those CLI internals: `EvidenceRunCommand` injects `CoverageRunWorkflow` and calls the static gate evaluator. That proves the product path, but it leaves the reusable engine in the wrong assembly and couples EvidenceHost to CLI implementation details.

The result should be a single private coverage engine that the public coverage commands and the first-party Evidence coverage producer call in-process. Existing CLI behavior remains authoritative and unchanged at its public boundary.

## What Makes This Cool

EvidenceHost becomes trustworthy for coverage because it is not an imitation of the CLI. The same discovery, scheduling, process supervision, Cobertura merge, patch-target analysis, threshold evaluation, artifacts, and cleanup run once in both paths. A coverage claim is therefore evidence about the command users already rely on, not about a parallel runner that happens to look similar in happy-path tests.

## Constraints

- The extraction is implementation-only. It adds no public coverage orchestration API and no consumer extension point.
- Do not shell out from Evidence to `appsurface`; use the extracted code in-process.
- Preserve `coverage run`, `coverage merge`, `coverage gate`, and `coverage clean` command syntax, defaults, artifacts, exit semantics, diagnostics, scheduling, watchdog behavior, and cleanup behavior.
- Keep `CliFx` commands, option parsing, stdout/GitHub-summary presentation, and command-specific exception mapping in `ForgeTrust.AppSurface.Cli`.
- Keep Evidence contracts and `IEvidenceProducer` translation outside the coverage core. The core must not depend on `Evidence.Contracts`, `Evidence.Planner`, `Evidence.Aspire`, or consumer code.
- Replace every core `CommandException` use with one internal structured coverage failure type carrying the existing stable code, rendered message, and optional exit code. The CLI alone maps that type back to the exact current `CommandException` contract.
- Preserve the current explicit `--diff-file` flow. A patch gate in `evidence run` must continue to use the same bounded unified-diff artifact that planning consumed; normalized changed paths alone are insufficient for line-level patch coverage.
- The Evidence result must remain bounded and secret-safe. It may name controlled relative coverage artifacts and stable failure classes, but must not place raw test logs or host paths in the manifest diagnostic.
- The existing `ReportGenerator` tool payload remains owned by the CLI tool package. The extracted runtime locates that known payload through the host layout; it must not create a second tool-payload distribution path.
- Tests use public or intentionally friend-visible seams only. No reflection or wall-clock sleeps.

## Premises

1. One implementation must own run, merge, gate, and artifact safety. A facade that independently reconstructs either the runner or gate violates the feature's reason for existing.
2. A pair of caller-owned `TextWriter` values is a sufficient shared progress boundary. `IConsole` is a CLI adapter concern; normal progress must retain its existing stdout routing and watchdog/critical diagnostics must retain stderr routing.
3. `ForgeTrust.AppSurface.Evidence.Cli` is the first-party adapter layer. It can translate a core result to `EvidenceProducerResult` because it already owns Evidence planning/CLI workflow code, while `Evidence.Aspire` remains a generic explicit-registration lifecycle host.
4. The package graph may carry an internal implementation assembly as a dependency, but every type intended for that assembly's consumers remains `internal`. Publishing a package artifact is not permission to publish an API contract.

## Approaches Considered

### A. Project-cut extraction

Move the coverage code almost unchanged and retain its `CliFx.IConsole` dependency. This is fast, but turns CLI presentation plumbing into a permanent dependency of the reusable engine. Rejected because it makes the wrong boundary easy to preserve.

### B. Cohesive private coverage core — selected

Move the mutually coupled coverage implementation to `ForgeTrust.AppSurface.Evidence.Coverage`, replace its `IConsole` dependency with `TextWriter`, and keep CLI and Evidence adapters thin. This is the smallest boundary that retains one engine while preventing CLI and Evidence contracts from leaking into each other.

### C. Public coverage contracts package

Create public request/result abstractions before extraction. Rejected because no external consumer has demonstrated a compatibility need, and #776 explicitly prohibits a new general-purpose coverage API.

## Recommended Approach

### Package and dependency graph

Add two implementation projects and keep dependency direction inward:

```text
ForgeTrust.AppSurface.Cli ────────────────> ForgeTrust.AppSurface.Evidence.Cli ──> Evidence.Contracts
            │                                           │
            └───────────────────────────────────────────┼──────────────> Evidence.Coverage
                                                        │
                                                        └──> Evidence.Contracts

ForgeTrust.AppSurface.Evidence.Aspire ───────────────────────────────────> Evidence.Contracts

Evidence.Coverage ──> CliWrap and BCL only
  no reference to CLI, CliFx, Evidence.Contracts, Evidence.Planner,
  Evidence.Cli, Evidence.Aspire, or consumer assemblies
```

Each arrow denotes a `ProjectReference`. The CLI is the only composition root that supplies CLI-owned implementations. `ForgeTrust.AppSurface.Evidence.Cli` references `Evidence.Coverage` to implement the first-party adapter, while `Evidence.Aspire` continues to reference only Evidence contracts and explicit lifecycle abstractions. It must not gain a dependency on the coverage core: it executes whichever typed producer the trusted first-party command/bootstrap registers.

The coverage project is shipped only as an implementation dependency of first-party packages. Its coverage types stay `internal`; classify it only as a transitive support package, never as a direct-install/user-facing package-index offering. Its exact friend matrix is:

| Assembly | Allowed friends | Reason |
| --- | --- | --- |
| `ForgeTrust.AppSurface.Evidence.Coverage` | `ForgeTrust.AppSurface.Cli`, `ForgeTrust.AppSurface.Evidence.Cli`, `ForgeTrust.AppSurface.Cli.Tests` | Command adapters, first-party Evidence adapter, and the established consolidated coverage regression suite only. |
| `ForgeTrust.AppSurface.Evidence.Cli` | `ForgeTrust.AppSurface.Cli`, `ForgeTrust.AppSurface.Evidence.Cli.Tests` | Existing command assembly and adapter tests only. |
| `ForgeTrust.AppSurface.Cli` | `ForgeTrust.AppSurface.Cli.Tests` | Existing public-command regression test boundary. |

Existing direct workflow tests remain in the established `ForgeTrust.AppSurface.Cli.Tests` coverage regression suite, which receives intentional first-party friend visibility alongside command contract tests. No dynamic-proxy or consumer-package friend is added. A compile-time package-API check verifies that the new assembly exposes no public coverage orchestration type or CliFx signature.

### Ownership split

| Area | Owner after extraction | Notes |
| --- | --- | --- |
| Command attributes, option validation, CLI help, command exit mapping, GitHub step-summary opt-in | `ForgeTrust.AppSurface.Cli` | Public command behavior remains here. |
| Coverage requests/results, discovery, driver preflight, scheduling, process runner, watchdog, merge, artifact reader/guard/lease, per-project manifest, gate evaluator/report writer, patch-diff parsing | `ForgeTrust.AppSurface.Evidence.Coverage` | Move coupled behavior together so no second logic path appears. |
| `CliFx.IConsole` to `TextWriter` adaptation | `ForgeTrust.AppSurface.Cli` | Pass `console.Output` to the core; core never references CliFx. |
| Evidence coverage run + gate composition and `EvidenceProducerResult` mapping | `ForgeTrust.AppSurface.Evidence.Cli` | The adapter consumes core internals through friend visibility and Evidence public contracts normally. |
| Explicit resource lifecycle and producer registration | `ForgeTrust.AppSurface.Evidence.Aspire` | Unchanged generic host contract. |
| ReportGenerator packed runtime payload | `ForgeTrust.AppSurface.Cli` | Preserve the existing .NET-tool package layout and locator behavior. |

### Core shape

Keep the existing coverage value types and workflows internal, moving them as a coherent unit rather than translating through newly public DTOs. Introduce one internal composition entry point for Evidence, conceptually:

```csharp
internal Task<CoverageEvidenceExecutionResult> RunAndGateAsync(
    CoverageRunRequest run,
    CoverageGateRequest gate,
    CoverageTextWriters writers,
    CancellationToken cancellationToken);
```

The entry point does not own command defaults or policy interpretation. It runs the existing workflow, stops before the gate when the run failed, evaluates the supplied gate request only after a successful merged Cobertura result, writes the existing controlled gate artifacts, and returns enough internal information for the adapter to select a stable evidence outcome.

`CoverageRunWorkflow` and `CoverageMergeWorkflow` remain the implementation owners of their present operations. The new composition entry point prevents the Evidence adapter from recreating the required ordering, but it must delegate to those workflows rather than duplicate their logic.

Replace the core's `IConsole` parameters, including watchdog output, with an internal `CoverageTextWriters(TextWriter Output, TextWriter Error)` record. Both writers are caller-owned: core code never disposes, flushes, or retains either after the operation finishes. Existing normal progress continues to `Output`; watchdog incidents and critical diagnostics continue to `Error`, preserving output routing and ordering. The core cannot write GitHub summaries, parse CLI options, construct `CommandException`, or determine process exit behavior. The CLI passes `new(console.Output, console.Error)`; focused tests pass two deterministic `StringWriter` instances and prove both routing and ordering.

Replace `CommandException` throughout moved run, merge, gate, watchdog, output-safety, and cleanup code with an internal `CoverageExecutionException`. It carries the existing rendered diagnostic and optional nonzero exit code, including the watchdog's `124`. A CLI-only `CoverageCommandExceptionMapper` converts it to the same `CommandException` message and exit code at each command boundary. Core tests assert the structured coverage failure; CLI regression tests assert byte-for-byte stable public diagnostics and exit behavior.

`Evidence.Coverage` owns `ICoverageRunReportGenerator`, `CoverageRunReportGenerator`, and `IReportGeneratorPackageLocator` as internal merge abstractions. The CLI keeps the `ReportGenerator` package reference, `GeneratePathProperty`, packed `tools/.../reportgenerator` payload, and concrete `ReportGeneratorPackageLocator`. The CLI composition root registers that locator and one report-generator implementation once; both ordinary coverage commands and `CoverageEvidenceProducer` receive the same core services. `Evidence.Cli` must not add a ReportGenerator package reference, copy the payload, or probe a second package layout.

Move the following coupled source units with their tests, splitting only their command adapters from their execution code:

- `CoverageRun.cs`: keep `CoverageRunCommand` and `CoverageRunDiagnostics` in CLI; move request/result models, `CoverageRunWorkflow`, process execution, output guard, sandbox guard, and internal helpers. Replace core `CommandException` paths with `CoverageExecutionException`.
- `CoverageMerge.cs`: keep the command wrapper in CLI; move merge request/result/workflow, source resolver, output guard, path-safety, staging, and report-generator abstractions. Replace core command exceptions with coverage failures.
- `CoverageGate.cs`: keep command option parsing, command-level diff-source selection, GitHub-summary opt-in/appending, and CLI failure mapping in CLI; move the core-only gate request/model, Cobertura evaluator, patch parser, artifact/report writer, diff readers, and output-path policy to core. Remove `WriteGithubSummary` and `GithubStepSummaryPath` from the core request. The core writes the bounded Markdown artifact; the CLI-only summary adapter appends that artifact when requested and maps its existing `ASCOV008` failure. Replace core command exceptions with coverage failures.
- `CoverageProjectManifest.cs`, `CoverageRunArtifactReader.cs`, `CoverageRunDrivers.cs`, `CoverageRunOutputLease.cs`, `CoverageRunSlowTestDiagnostics.cs`, `CoverageRunWatchdog.cs`, and any coverage-only filesystem interop to core.
- Keep `CoverageCleanCommand` in CLI, but make it consume the moved output-safety types so its current behavior remains part of the same safety model.

The destination namespace is `ForgeTrust.AppSurface.Evidence.Coverage` for every moved type. Private and file-local helpers move with their owner. No type may have two owners, no file in the core may reference CliFx, and no CLI command-layer file may retain coverage execution, merge, gate evaluation, patch parsing, or artifact-write logic.

| Source unit | CLI remains | Core owns after move |
| --- | --- | --- |
| `CoverageRun.cs` | `CoverageRunCommand`, CLI exception mapper/diagnostics | `CoverageRunRequest`, `CoverageRunResult`, schedules/projects/process models, run workflow, process runner, merge abstraction/implementation, report-generator interface/locator interface, output/sandbox guards, all execution helpers |
| `CoverageMerge.cs` | `CoverageMergeCommand` | `CoverageMergeRequest`, `CoverageMergeResult`, merge workflow, source resolver, output guard, path safety, staging |
| `CoverageGate.cs` | `CoverageGateCommand`, flag validation, CLI diff-source construction, GitHub-summary appender and `ASCOV008` mapping | gate/patch request and result models, evaluator, patch parser, diff reader, repository-root resolver, report/artifact writer, output-path policy |
| `CoverageRunWatchdog.cs` | none | watchdog mode/configuration, text-writer sink, supervisor, operation, process lease, incident helpers |
| Remaining coverage support files | `CoverageCleanCommand` only | project manifest, artifact reader, driver strategy/preflight, output lease, slow-test diagnostics, coverage-only filesystem interop |

### Evidence producer path

Add an internal `CoverageEvidenceProducer` to `ForgeTrust.AppSurface.Evidence.Cli`. `EvidenceRunCommand` parses and resolves CLI inputs into one immutable `CoverageEvidenceRequest`; it contains every currently hard-coded Evidence coverage default, the selected producer declaration, solution path, controlled output directory, caller-owned `CoverageTextWriters`, deadline, and resolved diff snapshot. The producer receives only that request plus the extracted core composition service:

- repository/solution path and controlled producer output directory;
- the selected producer declaration and its required `EvidenceCoverageGateRequirements`;
- the same immutable diff snapshot used for the plan when the declaration requires patch thresholds; and
- the CLI output writer and producer deadline/cancellation token.

The producer maps the explicit request to `CoverageRunRequest` and `CoverageGateRequest`; it invents no defaults and owns no CLI option parsing. It maps results exactly:

- successful run and passing gate → `Passed` with the declaration's assertion IDs;
- run failure, merge/artifact failure, gate failure, or safe nonfatal core exception → `Failed` with no assertions;
- missing solution, missing gate requirements, or required patch gate without `--diff-file` → `Unavailable` or `Invalid` with the present stable, payload-free diagnostic intent;
- producer deadline expiry → `TimedOut` with no assertions;
- caller cancellation → propagate cancellation to the EvidenceHost lifecycle, which already emits the host-owned cancellation outcome.

The producer should not emit raw coverage log content into the evidence manifest. If a policy declares coverage artifacts, copy only those declared files through the provided `EvidenceArtifactWriter`; otherwise keep existing coverage artifacts inside the controlled producer output directory and expose only their safe relative names in command output.

`EvidenceRunCommand` becomes a command adapter: it asks `EvidenceCliWorkflow` for the plan, delegates coverage work to `CoverageEvidenceProducer`, and retains plan/manifest file writing plus CLI-specific summary output. It no longer references `CoverageRunWorkflow` or `CoverageGateEvaluator` directly.

### Immutable patch-diff snapshot

For `evidence run`, `EvidenceCliWorkflow.ResolveRunAsync` reads `--diff-file` once before planning, applies the existing byte limit and regular-file/path policy, and returns an internal `EvidencePlanningResolution(EvidencePlan Plan, EvidenceDiffSnapshot? DiffSnapshot)`. The `Evidence.Cli`-owned snapshot contains exact bytes, safe display name, byte count, and SHA-256. Planning derives normalized changed paths from that snapshot. Because the core cannot reference Evidence.Cli types, the producer passes the snapshot's primitive bytes, display name, length, and hash to the internal `PatchDiffSource.ForSnapshot(...)` factory in Evidence.Coverage; that factory serves the same bytes and never reopens the filesystem path.

The snapshot is retained only for the bounded command execution. File replacement, deletion, relative-path retargeting, or symlink retargeting after planning cannot alter the gate's measured input. Snapshot acquisition failures retain the existing Evidence CLI diagnostic family; a missing required snapshot makes the producer return `Unavailable` with no assertions. No evidence claim may pass after falling back to a second file read. `evidence explain` and `evidence doctor` may call the shared input resolver and discard their snapshot after planning because they never gate coverage.

### Diagnostic and composition seams

`Evidence.Coverage` owns an internal `CoverageExecutionDiagnostic` record with the exact rendered message, stable code when one exists, and optional exit code, plus `CoverageExecutionDiagnostics.Create(...)` to render the present problem/cause/fix/docs/log form. `CoverageExecutionException` carries that record. Every moved `CommandException` site becomes this core exception, including merge/output safety, gate parsing/evaluation, ReportGenerator resolution, and watchdog terminal failure. `CoverageCommandExceptionMapper`, owned by CLI, is the *only* code that creates `CommandException`; it preserves the existing message and the watchdog's exit code `124`. No moved code calls `CoverageRunDiagnostics` after extraction.

`Evidence.Coverage` also owns a transient internal `CoverageEvidenceExecutionWorkflow`. Its constructor receives the registered `CoverageRunWorkflow` and a new internal `CoverageGateWorkflow` that delegates to the existing evaluator/report writer. `RunAndGateAsync` accepts fully resolved run/gate requests plus `CoverageTextWriters`, invokes the run workflow once, stops on a failed run, invokes the gate workflow only after success, and returns a core result without Evidence contracts. `AppSurfaceCliApp` registers one singleton each of `ICoverageRunProcessRunner`, `IReportGeneratorPackageLocator`, and `ICoverageRunReportGenerator`; one transient each of `CoverageRunWorkflow`, `CoverageMergeWorkflow`, `CoverageGateWorkflow`, `CoverageEvidenceExecutionWorkflow`, and `CoverageEvidenceProducer`; and no coverage service in `Evidence.Aspire`.

Ordinary `coverage run` and `coverage merge` command adapters continue to call their named core workflows directly. `coverage gate` calls the core gate workflow then the CLI-only GitHub-summary adapter when selected. `evidence run` is the only command that calls `CoverageEvidenceProducer`, which calls the same registered `CoverageEvidenceExecutionWorkflow`; it does not construct a second process runner, report generator, run workflow, or gate evaluator.

### Migration order

1. Add `Evidence.Coverage` to the solution, project locks, package validation graph, and internal-friend declarations. Keep direct core behavior in the established first-party coverage regression suite. Do not change command behavior yet.
2. Move coverage execution types as a dependency-closed set. Replace `IConsole` with caller-owned `CoverageTextWriters` at the core boundary. Keep all existing requests, artifact names, diagnostics, stdout/stderr routing, and file layouts intact.
3. Change CLI composition in `AppSurfaceCliApp` and the four coverage commands to use the core. Register exactly one core composition service, one CLI-owned `ReportGeneratorPackageLocator`, one report-generator implementation, and one Evidence adapter. Retain command options, help text, command exception mapping, and ReportGenerator tool payload ownership. `Evidence.Aspire` receives no coverage-core registration.
4. Keep focused workflow, guard, watchdog, and manifest tests in the existing friend-visible coverage regression suite. Continue to assert exact public diagnostics through command-level tests.
5. Add `CoverageEvidenceProducer` and immutable-snapshot tests. Change `EvidenceRunCommand` to delegate to that producer and remove its direct CLI-coverage references.
6. Add packed-consumer tests that restore/build the CLI plus internal dependency graph and run the existing package coverage proof. Confirm the actual tool layout still locates ReportGenerator and produces the canonical merge/gate artifacts.
7. Update package, CLI, and Evidence documentation plus the append-only release entry. State explicitly that normal `coverage` commands remain compatible and that `Evidence.Coverage` is not a supported consumer API.

### Verification matrix

| Proof | Required checks |
| --- | --- |
| Core workflow unit tests | discovery, explicit projects, driver preflight, schedule ordering, watchdog warnings/failures, cancellation, test failure, missing/malformed artifacts, merge failure, output safety, cleanup, structured failure codes, and caller-owned stdout/stderr `TextWriter` routing preserve the existing branches. |
| Gate unit tests | overall thresholds, tolerance, patch-line modes, bounded diff sources, malformed diff/XML, path normalization, patch targets, safe report output, and all current stable diagnostics. |
| CLI regression tests | existing `coverage run`, `merge`, `gate`, and `clean` flags, defaults, exit conditions, stdout/stderr wording and ordering, artifact names, GitHub-summary behavior including `ASCOV008`, and package payload lookup remain unchanged. |
| Evidence producer tests | pass, test/merge/artifact failure, gate failure, missing `--solution`, absent `coverageGate`, missing patch diff, timeout, nonfatal exception, exact assertion closure, no raw-log leakage, and immutable diff snapshot reuse after replacement/deletion/symlink retargeting. |
| Integration/DI tests | command app registers one core composition service, one CLI locator, one report generator, and one Evidence adapter; `evidence run` has no direct workflow/evaluator reference; `Evidence.Aspire` has no core reference; no subprocess is launched. |
| Packed consumer tests | restore/build supported first-party package graph; execute the existing semantic coverage proof; prove the CLI package contains exactly one ReportGenerator payload and both ordinary coverage and in-process Evidence runs resolve that same payload. |

## Success Criteria

1. The CLI and Evidence producer execute the same extracted run/merge/gate implementation in-process.
2. The core has no `CliFx`, Evidence contract, Aspire, or consumer dependency; all intended API remains internal and the exact friend matrix is enforced.
3. `evidence run` contains no direct `CoverageRunWorkflow` injection or direct `CoverageGateEvaluator` call.
4. Every current coverage command retains its public contract, controlled artifacts, exit semantics, diagnostics, watchdog behavior, scheduling, and cleanup behavior.
5. A coverage declaration can close its assertions only after the extracted workflow succeeds and the existing gate passes.
6. Patch coverage uses the same immutable bounded diff snapshot consumed by Evidence planning; source mutation after planning and missing required diff input cannot create a passing claim.
7. Core, adapter, CLI regression, and packed-consumer tests cover success, failure, timeout, cancellation, safety, and compatibility branches without reflection.
8. Documentation and release notes make the private boundary and rollback path clear: removing Evidence adoption leaves ordinary coverage commands unchanged.

## Distribution Plan

The existing AppSurface package and release pipeline distributes this work. `ForgeTrust.AppSurface.Evidence.Coverage` travels only as a first-party implementation dependency, classified as a transitive support package rather than a direct-install package-index offering or supported integration surface. The CLI remains the .NET tool that carries the ReportGenerator payload. Existing package validation and packed-consumer checks become the release gate for the new dependency layout.

## Dependencies

- [#775](https://github.com/forge-trust/AppSurface/pull/775) is merged at `f9d33b48d568baf4ea1fa59cb7f2fd0ce7a4aa29`; implementation begins from that code, not this checkout's older `main` state.
- [#777](https://github.com/forge-trust/AppSurface/issues/777) remains downstream. It should use the extraction rather than drive a separate coverage implementation.
- The existing CLI package proof and ReportGenerator payload contract must be preserved; no new external tool installation is part of this feature.

## Next Steps

1. Update the local branch to the merged #775 base before creating the feature branch.
2. Introduce the core project and move the dependency-closed coverage implementation, first preserving existing CLI tests.
3. Convert the core output boundary to `TextWriter` and add the CLI adapter regression tests.
4. Add the internal Evidence producer adapter and replace direct coverage references in `EvidenceRunCommand`.
5. Complete package-layout, packed-consumer, documentation, release-note, formatting, analyzer, and solution-coverage verification.

## The Assignment

Implement the extraction as one focused PR after rebasing on #775. Its reviewable proof should contain three short answers: which command wrappers remain in CLI, which coverage units moved to `Evidence.Coverage`, and one test showing the Evidence producer reaches the same run-and-gate path without launching `appsurface`.

## What I noticed about how you think

- You asked for the **"cleanest implementation"**, not the fastest migration. That made the right trade clear: remove the `CliFx` dependency at the core boundary instead of preserving a convenient but leaky shortcut.
- You chose the cohesive private core only after the failure modes were explicit: copied coverage logic, a second process, and a premature public API all stay out of the feature.

## Autoplan Phase 1 — CEO Review

### Premise confirmation

The human premise gate is satisfied by the explicit `A` approval of this design before `/autoplan` began. The review therefore treats the selected private-core approach as an approved direction rather than silently replacing it. The only additional prerequisite is factual rather than strategic: implementation must first update from this checkout's `6560ea6e` to merged #775 (`f9d33b48d568baf4ea1fa59cb7f2fd0ce7a4aa29`).

### 0A. Premise challenge

The problem is real: #775 proved that Evidence can call the existing CLI workflow, but that direct assembly coupling prevents a coherent first-party Evidence path. A subprocess adapter would be operationally easier but would weaken timeouts, artifacts, cancellation, and diagnostics into a second contract. A public contract package would pre-commit to compatibility before any external consumer demonstrates a need. The approved private implementation core remains the direct route to an evidence claim that measures the same behavior users invoke.

### 0B. What already exists

| Sub-problem | Existing leverage | Review decision |
| --- | --- | --- |
| Coverage execution | `CoverageRunWorkflow`, drivers, watchdog, artifact readers, output guards | Move as a dependency-closed unit; do not reimplement. |
| Coverage merge | `CoverageMergeWorkflow`, ReportGenerator abstractions and locator | Keep the package payload and concrete locator in CLI; move only the shared abstractions/workflow. |
| Coverage gate | `CoverageGateEvaluator`, patch parser, report writer | Move evaluation and safe artifact creation; leave flags, GitHub summary append, and CLI error mapping in CLI. |
| Evidence planning | #775 `EvidenceCliWorkflow` and contracts | Preserve its ownership of planning/defaults and add one first-party producer. |
| Package proof | Existing packed CLI semantic coverage proof | Extend it to prove exactly one ReportGenerator payload and one in-process engine. |

### 0C. Dream-state delta

```text
CURRENT: CLI owns execution; Evidence reaches into CLI internals
   --> #776: internal engine with thin CLI and Evidence adapters
       --> 12-MONTH IDEAL: first-party producers reuse bounded internal engines
           without public APIs or host-specific subprocess adapters
```

### 0C-bis. Alternatives

| Approach | Completeness | Effort | Risk | Result |
| --- | ---: | --- | --- | --- |
| Keep direct CLI references | 3/10 | S | High coupling | Rejected: #775's tactical seam becomes permanent architecture. |
| Shell out to `appsurface` | 5/10 | M | Duplicate lifecycle and weak diagnostics | Rejected: a second process makes evidence less trustworthy. |
| Selected private core | 10/10 | L (human ~3 days / CC ~1h) | Migration breadth | Accepted: one engine, private boundary, public CLI preservation. |
| Public contracts first | 6/10 | L | Premature compatibility burden | Rejected: no proven external consumer. |

### 0D. Selective-expansion analysis

The plan touches more than eight files, but that is necessary dependency closure rather than avoidable feature breadth. The small implementation expansion that pays for itself is a compile-time public-surface check: it prevents an accidental public type or `CliFx` signature from escaping in a package that is intentionally private. Broader generalized producer infrastructure, an external consumer API, a second tool payload, or an Aspire-owned coverage registration are outside the blast radius and remain deferred.

### 0E. Temporal interrogation

| Implementation point | Decision now |
| --- | --- |
| Foundation | Update to #775 before adding project references; retain exact friend matrix. |
| Core move | Move all `CommandException` sites with a structured core diagnostic, never piecemeal. |
| Integration | Register one process runner, one locator, one report generator, and one composition workflow. |
| Evidence adapter | Read `--diff-file` once, carry immutable bytes to `PatchDiffSource.ForSnapshot`, and never reopen it. |
| Proof | Preserve stdout/stderr ordering, watchdog exit `124`, `ASCOV008`, package payload lookup, and packed-consumer behavior. |

### CEO dual voices

The `combo/sub` CEO agent was started with independent, read-only instructions but exceeded its bounded window and was stopped. The separate read-only Codex strategy voice inspected the design and did not identify a premise that overturns the approved approach; its evidence-backed emphasis was preserving the actual package layout and beginning from #775, both already required here.

| Dimension | Subagent | Codex | Consensus |
| --- | --- | --- | --- |
| Premises valid? | unavailable | yes | N/A, primary review confirms |
| Right problem? | unavailable | yes | N/A, primary review confirms |
| Scope calibrated? | unavailable | yes, update to #775 first | N/A |
| Alternatives explored? | unavailable | yes | N/A |
| Risk coverage? | unavailable | package/compatibility emphasis | N/A |
| Six-month trajectory? | unavailable | private core avoids contract debt | N/A |

### Error & Rescue Registry

| Codepath | Failure | Core result/rescue | User/host outcome |
| --- | --- | --- | --- |
| Snapshot resolution | absent, oversized, non-regular, replaced path | Evidence CLI diagnostic; no second read | `Unavailable`/`Invalid`, no assertions |
| Run/merge | process, artifact, output-safety, watchdog failure | `CoverageExecutionException` with stable code/exit | CLI maps exact `CommandException`; producer `Failed` |
| Gate | malformed input/XML, threshold miss, unsafe output | structured core failure or failed gate result | bounded artifact, no assertions |
| Producer deadline | deadline expiry | cancellation/timeout boundary | `TimedOut`, no assertions |
| Caller cancellation | host cancellation | propagated, never relabeled | host-owned cancellation outcome |

### Failure Modes Registry

| Codepath | Failure mode | Rescued? | Test? | User sees | Logged? |
| --- | --- | --- | --- | --- | --- |
| Package layout | second ReportGenerator payload or failed lookup | Y | required packed proof | existing package diagnostic | Y |
| Diff flow | planning and gate use different bytes | Y | required mutation/symlink tests | unavailable/failed claim | Y |
| CLI boundary | changed stdout/stderr or exit `124` | Y | required regression tests | byte-stable current behavior | Y |
| Evidence boundary | raw logs leak into manifest | Y | required producer test | payload-free diagnostic | Y |
| DI | duplicate workflows/runner construction | Y | required composition test | deterministic in-process run | Y |

No row is permitted to be silent, untested, and unrescued.

### Sections 1–10 review

1. **Architecture:** the inward project graph and exact friends prevent CLI, Aspire, and public contract leakage; keep the dependency graph in the design as the implementation contract.
2. **Errors:** the named core failure model closes the current `CommandException` coupling; preserve existing rendered messages and exit codes at CLI boundaries.
3. **Security:** the main new trust boundary is file input and manifest output; immutable diff bytes, bounded artifacts, and no raw logs address it without adding an authorization surface.
4. **Data flow:** the run-to-gate path has explicit missing, empty, failure, and success outcomes; the required snapshot tests cover post-plan replacement, deletion, and symlink retargeting.
5. **Quality:** a cohesive move is cleaner than facade duplication; the `CoverageTextWriters` pair is an explicit narrow seam rather than a new abstraction hierarchy.
6. **Tests:** every moved branch needs a core unit test, while public behavior stays covered by CLI regressions and package proof.
7. **Performance:** the design adds no second process, no repeated diff read, and no duplicated ReportGenerator discovery; this is the preferred performance posture.
8. **Observability:** keep stable diagnostics and controlled artifacts; do not add a dashboard or general telemetry system to this extraction.
9. **Deployment:** package validation and packed-consumer proof are the rollout gate; rollback is removing Evidence adoption while retaining unchanged CLI commands.
10. **Trajectory:** reversibility is 4/5 because the private core is internal, but extraction sequencing must not land a half-moved graph.

### NOT in scope

- A public coverage SDK or package-index product: no demonstrated external compatibility need.
- A generic CI/evidence platform: #776 only makes the first-party producer truthful.
- An Aspire coverage-core dependency or registration: the host remains generic.
- New dashboards, metrics, feature flags, or deployment controller behavior: not necessary to preserve the existing CLI contract.
- Synthetic multi-project coverage fixtures: retain the existing proof and defer broader fixtures until a demonstrated graph regression.

### CEO Completion Summary

| Item | Result |
| --- | --- |
| Mode | Selective expansion, with only the public-surface check added in blast radius |
| Critical gaps | 0, if the required package and snapshot proofs land |
| Architecture | private friend-visible core approved |
| Error/failure registries | written |
| UI review | skipped: no user-facing visual scope |
| Outside voices | Codex completed; `combo/sub` timed out and is recorded |
| Unresolved premises | 0; user approved the design before autoplan |

### Decision Audit Trail

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | CEO | Retain the approved cohesive private core | Mechanical | P1, P4 | One engine is required for evidence truthfulness | direct CLI coupling, subprocess, public contract |
| 2 | CEO | Treat #775 update as an implementation precondition | Mechanical | P5 | The target Evidence projects do not exist in this checkout | implementing against stale main |
| 3 | CEO | Add package public-surface verification | Mechanical | P2 | It is a small direct safeguard against accidental API publication | trusting review alone |
| 4 | CEO | Keep Aspire free of core registration | Mechanical | P5 | Generic host lifecycle must not know a coverage implementation | host-specific coverage coupling |

## Autoplan Phase 3 — Engineering Review

### Scope challenge and code evidence

The extraction is broad but not scope creep: `CoverageRunWorkflow` begins at `Cli/ForgeTrust.AppSurface.Cli/CoverageRun.cs:556`, `CoverageMergeWorkflow` at `CoverageMerge.cs:87`, and `CoverageGateEvaluator` at `CoverageGate.cs:801`; their public command adapters are at the heads of those files. `AppSurfaceCliApp.cs:76-89` already centralizes one process-runner, ReportGenerator locator, report generator, and the transient coverage workflows. The plan must preserve this singleton/transient topology rather than let the Evidence producer construct implementations. The implementation must also include `CoverageFileSystemInterop.cs`, which the initial source list omitted from the prose but which the repository uses as coverage-only filesystem support.

### Engineering dual voices

The independent `combo/sub` engineering agent was started read-only but exceeded its bounded window and was stopped. The Codex engineering voice was also invoked read-only; it completed repository inspection but its final response was not persisted by the CLI. Primary code inspection supplies the evidence below, and this phase is explicitly degraded to single-reviewer rather than inventing an outside verdict.

| Dimension | Subagent | Codex | Primary result |
| --- | --- | --- | --- |
| Architecture sound? | unavailable | unavailable final | yes, if composition remains CLI-owned |
| Test coverage sufficient? | unavailable | unavailable final | no until moved-branch, CLI, adapter, and package proof layers exist |
| Performance risks addressed? | unavailable | unavailable final | yes: no second process/read/payload scan |
| Security threats covered? | unavailable | unavailable final | yes: bounded snapshot and artifact rules |
| Error paths handled? | unavailable | unavailable final | yes if all moved diagnostics migrate atomically |
| Deployment risk manageable? | unavailable | unavailable final | yes with package validation and packed proof |

### Architecture dependency graph

```text
CliFx command adapters + CLI exception/summary mapper
             |                                  \\
             v                                   \\ CLI package payload locator
Evidence.Coverage (internal) <--- friend --- Evidence.Cli producer
  RunWorkflow -> MergeWorkflow -> GateWorkflow       |
  TextWriters / diagnostics / PatchDiffSource         v
             |                                  Evidence.Contracts
             v
      CliWrap + BCL

Evidence.Aspire ---> Evidence.Contracts only
```

### Test diagram

```text
CORE CODE PATHS                                PUBLIC/INTEGRATION PATHS
Run request                                    coverage run/merge/gate/clean flags
  + valid projects -> reports -> merge         + stdout/stderr exact routing
  + no projects / cancellation / failure        + watchdog exit 124
  + malformed/missing output                    + GitHub summary ASCOV008
  + watchdog warning/terminal failure           + locator resolves packaged payload

Gate request                                   Evidence producer
  + overall and patch thresholds                 + run succeeds then gate passes => assertions
  + malformed XML/diff/path                      + run/merge/gate failure => no assertions
  + immutable snapshot mutation paths            + missing solution/gate/diff => unavailable
                                                 + deadline/cancellation and no raw logs

PACKAGE PROOF
  packed CLI -> exactly one ReportGenerator payload -> ordinary CLI and Evidence use it
```

### Findings and decisions

1. **[P1, confidence 10/10] Dependency closure:** include `CoverageFileSystemInterop.cs` with the move. Leaving it behind would make the core depend on CLI filesystem behavior. Decision: add it to the moved-unit list and core tests.
2. **[P1, confidence 10/10] Service topology:** `AppSurfaceCliApp.cs:76-89` already holds the shared registrations. Decision: core receives the CLI-supplied locator/report-generator; no Evidence-side implementation construction or duplicate singleton.
3. **[P1, confidence 9/10] Test ownership:** workflow tests currently live under `Cli/ForgeTrust.AppSurface.Cli.Tests` alongside public command tests. Decision: keep the existing consolidated first-party regression suite as an intentional friend of the private core, preserving its security and behavior fixtures while command tests continue asserting the public CLI contract.
4. **[P1, confidence 9/10] Package graph proof:** package metadata and `verify-packages` already treat ReportGenerator as CLI-owned. Decision: extend that existing semantic proof, not a new packing path.
5. **[P2, confidence 8/10] Migration safety:** compilation can succeed with stale internal access until packaging. Decision: make the package API/CliFx-reference check and packed-consumer proof mandatory before the change is considered complete.

### Performance, security, and rollout

There are no database or network fan-out paths. The principal performance regressions would be repeated diff reads, a second child `appsurface` process, or independent ReportGenerator discovery; all are forbidden by the plan. Security is file-boundary and output-bound: regular-file checks, byte limits, immutable snapshot bytes, safe relative artifacts, and payload-free manifest diagnostics stay mandatory. Rollout is package validation plus the existing local packed-consumer verification; rollback removes the Evidence producer adoption while ordinary CLI coverage commands continue to use the same core.

### Worktree strategy

| Step | Modules | Depends on |
| --- | --- | --- |
| Core project and moved tests | `Evidence/`, `Cli/` coverage sources | #775 base |
| CLI adapters and DI | `Cli/` | core project |
| Evidence producer | `Evidence/` CLI adapter | core API and DI |
| Package proof/docs | `packages/`, `tools/`, docs | all behavior complete |

Sequential implementation is recommended: the first three lanes share the core boundary and contract-preservation tests. Package/documentation proof can begin only after the project graph stabilizes.

### Engineering Completion Summary

| Item | Result |
| --- | --- |
| Architecture | dependency graph and ownership boundary written |
| Code quality | one cohesive move, no wrapper duplication |
| Tests | branch-level core, adapter, CLI and package proof matrix written |
| Performance | no new process or repeated diff/payload lookup |
| Critical gaps | 0 after adding filesystem interop and required proof layers |
| Outside voices | both unavailable after bounded attempts; degraded status recorded |

### Decision Audit Trail (continued)

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
| --- | --- | --- | --- | --- | --- | --- |
| 5 | Eng | Move `CoverageFileSystemInterop.cs` with coverage core | Mechanical | P5 | It is coverage-only support required for true dependency closure | CLI-to-core hidden dependency |
| 6 | Eng | Keep DI construction in CLI composition root | Mechanical | P4, P5 | One shared runner/locator/report generator preserves payload ownership | Evidence-side duplicate construction |
| 7 | Eng | Split tests by implementation versus public boundary | Mechanical | P1 | Internal core needs direct branch coverage; CLI keeps exact external contract tests | reflection or broad friend access |
| 8 | Eng | Extend existing `verify-packages` proof | Mechanical | P4 | Existing proof owns CLI payload validation | a second package/proof workflow |

## Autoplan Phase 3.5 — Developer Experience Review

### DX scope and developer journey

Primary product type: **CLI Tool plus first-party library/package integration**. The developer is an existing AppSurface CLI or EvidenceHost maintainer, not a new external coverage-SDK adopter. Their journey is: discover the existing `coverage` and `evidence` commands; update to the #775 base; build the packages; run the familiar CLI commands; run `evidence run`; diagnose a failure from stable codes/artifacts; upgrade the package; and validate it in CI through the existing package proof.

| Stage | Developer does | Friction guarded by this plan | Status |
| --- | --- | --- | --- |
| Discover | reads CLI/Evidence docs | could mistake the core for a supported package | fix: docs state internal-only |
| Install | uses existing CLI package | could gain a duplicate payload | fix: one-payload packed proof |
| Hello world | invokes existing coverage command | command behavior could drift during extraction | fix: exact CLI regression layer |
| Real usage | runs `evidence run` | could believe producer is a subprocess | fix: in-process producer/DI test |
| Debug | reads existing diagnostics and safe artifacts | core exception could leak a new error contract | fix: CLI-only mapper and regression tests |
| Upgrade | updates first-party packages | private implementation could appear as an adoption target | fix: package/README/release guidance |
| CI | executes package proof | core dependency graph could fail only when packed | fix: mandatory `verify-packages` proof |
| Extend | considers a new producer | could depend on coverage core directly | fix: Aspire/contracts remain generic |
| Recover | rolls back Evidence adoption | could fear coverage command rollback | fix: ordinary commands remain unchanged |

### DX findings and decisions

1. **[P1] Documentation:** update the CLI, Evidence, package-index, and release documentation in the same change to say that normal coverage commands retain their public contract and `Evidence.Coverage` is not a supported consumer API.
2. **[P1] Errors:** retain problem/cause/fix/docs/log diagnostic wording at the CLI boundary, and ensure Evidence emits stable payload-free diagnostic intent rather than raw test process output.
3. **[P1] CI confidence:** make the existing package consumer proof the copy-paste verification path, then extend it to show the shared payload and in-process producer path.
4. **[P2] Upgrade clarity:** the release entry must name the internal refactor, state there is no command migration, and identify #775 as the implementation base.

### DX scorecard

| Dimension | Initial | Target after implementation | Rationale |
| --- | ---: | ---: | --- |
| Getting started | 7/10 | 8/10 | existing commands remain familiar; package proof documents the supported path |
| API/CLI design | 8/10 | 9/10 | no public command or API change |
| Error messages | 7/10 | 9/10 | exact CLI mapping and payload-safe Evidence diagnostics |
| Documentation | 5/10 | 8/10 | required cross-surface documentation closes discoverability gap |
| Upgrade path | 6/10 | 8/10 | release note explicitly states no migration and #775 base |
| Dev environment/CI | 8/10 | 9/10 | existing packed-consumer proof becomes authoritative |
| Community/ecosystem | 7/10 | 7/10 | no external extension surface is intentionally added |
| DX measurement | 6/10 | 7/10 | package proof and stable artifacts provide a repeatable signal |

TTHW for an existing maintainer remains under five minutes for normal `coverage` commands; validating the full package graph is intentionally longer and CI-owned. No UI scope exists, so the visual-design phase remains skipped.

### DX implementation checklist

- [ ] Existing `coverage` help, flags, defaults, output, artifacts, and exit codes remain unchanged.
- [ ] `evidence run` states stable producer outcomes without raw-log leakage.
- [ ] CLI and Evidence documentation state that the core is internal-only.
- [ ] The release entry says no end-user command migration is required.
- [ ] Package proof demonstrates one ReportGenerator payload and shared in-process execution.
- [ ] Documentation links the ordinary CLI workflow, Evidence workflow, and package validation command.

### DX dual voices

The independent `combo/sub` DX agent exceeded its bounded window and was stopped. The read-only Codex pass inspected the CLI, package README, package index, and `CoverageCliConsumerProof` paths. Its visible evidence reinforces the plan's required documentation, release, and packed-consumer proof changes; no user-facing API expansion is warranted.

### Decision Audit Trail (continued)

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
| --- | --- | --- | --- | --- | --- | --- |
| 9 | DX | Document core as implementation-only in all affected surfaces | Mechanical | P1, P5 | Existing developers need a clear supported path without new API confusion | silent internal package publication |
| 10 | DX | Use existing package consumer proof as CI guidance | Mechanical | P4 | It already validates installed tool behavior and owned payloads | new bespoke onboarding workflow |
| 11 | DX | State no command migration in release documentation | Mechanical | P5 | Upgrade safety depends on clear compatibility messaging | implying a new user-facing feature |

## Cross-Phase Themes

- **One implementation, one package payload:** CEO, engineering, and DX all converge on eliminating duplicated execution and ReportGenerator distribution paths.
- **Public compatibility belongs at the CLI boundary:** strategy, test structure, diagnostics, and documentation agree that the core remains private while command behavior stays stable.
- **#775 is a hard sequencing dependency:** implementation and verification must run against the merged Evidence projects, not this stale checkout.

## Implementation Tasks

- [ ] **T1 (P1, human: ~6h / CC: ~30min)** — Coverage core — add the internal `Evidence.Coverage` project, move the dependency-closed units including `CoverageFileSystemInterop.cs`, and preserve the friend matrix.
  - Verify: friend-visible core behavior tests compile without `CliFx` or Evidence contract references in the core assembly.
- [ ] **T2 (P1, human: ~5h / CC: ~30min)** — CLI compatibility — adapt commands and DI to shared core services, map structured failures at the CLI boundary, and keep output/exit semantics.
  - Verify: focused CLI regression tests cover `coverage run`, `merge`, `gate`, and `clean` including watchdog and `ASCOV008`.
- [ ] **T3 (P1, human: ~4h / CC: ~25min)** — Evidence producer — add `CoverageEvidenceProducer`, immutable diff snapshot transfer, and one run-then-gate composition path.
  - Verify: producer tests cover assertions, failure, unavailable, timeout, cancellation, no raw logs, and source mutation.
- [ ] **T4 (P1, human: ~3h / CC: ~20min)** — Package and docs proof — extend packed-consumer verification, public-surface enforcement, docs, and release entry.
  - Verify: `verify-packages` and `./scripts/coverage-solution.sh` pass from the #775 base.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
| --- | --- | --- | --- | --- | --- |
| CEO Review | `/autoplan` | Scope and strategy | 1 | CLEAR | Private core retained; #775 sequencing prerequisite |
| Codex Review | `/autoplan` | Independent challenge | 3 | DEGRADED | Read-only inspections completed; final outputs were partially unavailable |
| Eng Review | `/autoplan` | Architecture and tests | 1 | CLEAR | Added filesystem interop, test ownership, and package-proof requirements |
| Design Review | `/autoplan` | UI/UX gaps | 0 | SKIPPED | No UI scope detected |
| DX Review | `/autoplan` | Developer experience | 1 | CLEAR | Documentation, release, and packed-consumer DX requirements added |

**CROSS-MODEL:** Independent agent calls timed out; available Codex inspection and primary review agree on private ownership, package proof, and #775 sequencing.

**VERDICT:** CEO + ENG + DX CLEARED for implementation after final user approval; implementation must update to #775 first.

NO UNRESOLVED DECISIONS

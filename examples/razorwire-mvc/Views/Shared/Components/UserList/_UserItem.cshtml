@model RazorWireWebExample.Services.UserPresenceInfo
<li id="user-@Model.Username" class="list-group-item py-1 d-flex justify-content-between align-items-center">
    <span>@Model.Username</span>
    <small class="text-muted">@Model.LastSeen.ToLocalTime().ToString("T")</small>
</li>

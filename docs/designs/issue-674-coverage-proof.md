# Design: Truthful packaged-consumer coverage proof for #674

Generated by `/office-hours` on 2026-08-18
Branch: `HEAD` (detached)
Repo: `forge-trust/AppSurface`
Status: APPROVED
Mode: Builder

## Problem Statement

[#674](https://github.com/forge-trust/AppSurface/issues/674) records a real consumer failure: Skoolit ran 2,986 passing Release tests against the AppSurface/RazorWire `0.2.0-preview.4` graph, but its per-project Cobertura report gave the directly exercised `Skoolit.Web` assembly zero line, branch, and method hits. The overall coverage gate consequently failed while patch coverage remained 100%.

That is not a coverage-quality result. It is a false reporting result that makes a release gate untrustworthy. The issue must establish whether the historical preview.4 failure remains live in a current package graph, then make the packaged CLI release proof detect the same class of false success.

## What Makes This Cool

A package release should prove that its coverage tool sees code that its own disposable consumer actually executes. The release gate moves from “an XML file exists” to “the raw report and the merged report both contain real hits for a known class and branch.” That is the small, satisfying invariant: a coverage tool cannot ship while silently measuring nothing.

## Constraints

- Preserve the #672 VSTest collector contract: direct `coverlet.collector` capability, unique collector results directories, one hardened normalized Cobertura attachment, and deterministic canonical artifact paths.
- Treat test engine and coverage integration as separate observed inputs. Record the consumer’s `global.json` runner configuration plus evaluated `TestingPlatformDotnetTestSupport` as `VSTest`, `MTP`, or invalid/unknown; independently record the direct `coverlet.collector`, `coverlet.msbuild`, and `coverlet.MTP` references as `collector`, `MSBuild`, `native`, missing, or conflicting. Derive the displayed workflow label from those two fields before attributing a failure to the collector.
- Do not lower total coverage thresholds, suppress the application assembly, or rely on patch coverage to mask a zero-hit report.
- Do not add MTP support, a public sentinel option, or an MSBuild-driver redesign unless captured consumer evidence proves the selected engine requires it.
- Keep the normal and explicit-MSBuild smoke paths. This issue strengthens the default collector proof; it must not silently broaden or remove compatibility coverage.
- Preserve private-by-default package evidence. Sanitized issue evidence may name the public consumer and package versions, but must not publish raw source, credentials, or unbounded logs.

## Premises

1. A passing project that reports 0% for a directly exercised application assembly has an instrumentation, report, or merge defect.
2. The current collector path remains the supported VSTest integration. Native MTP requires its own coverage integration and is a separate branch only when the consumer classification proves it is in use.
3. The first diagnosis must separate raw canonical per-project Cobertura from merged Cobertura. An all-zero or missing raw report points to collection, inclusion, normalization, or report-shape failure; valid raw hits lost in the merged report point to fan-in. Diagnostics must say which evidence supports that classification.
4. Preview.4 is useful historical baseline evidence. The latest coordinated AppSurface prerelease already published when the current evidence run starts determines whether #674 is still open; a later candidate is comparison evidence only.
5. The durable guard belongs in the existing packed CLI consumer proof because that proof already gates package publication and owns a direct `Smoke.Tests` to `Smoke` execution path.

## Cross-Model Perspective

An independent reviewer agreed that the first diagnostic must inspect raw per-project Cobertura before merge, and that the existing `Smoke` fixture is the right durable guard. It challenged one assumption: the Skoolit proof must capture whether it runs VSTest or MTP before it is described as a collector regression. The reviewer recommended asserting the exact `Smoke.Calculator` class, at least one covered line, exercised conditional branch coverage, and retention of those hits in the merged Cobertura report.

## Approaches Considered

- **Evidence-first closure:** rejected because a current passing Skoolit run alone would answer the issue but leave no release-time semantic guard against recurrence.
- **Consumer-declared coverage sentinels:** rejected because a new public CLI option expands the API and compatibility surface before an adopter has shown that the existing release proof is insufficient.

## Recommended Approach

Extend the existing pre-publish packaged CLI consumer proof with semantic Cobertura validation, while using a classified two-version consumer investigation to decide the issue outcome. For #674, **current package graph** means the latest coordinated AppSurface prerelease already published when the evidence run starts; a later locally packed or prerelease candidate is optional comparison evidence, never a substitute for that baseline.

### 1. Classify the external proof before executing it

For the recorded Skoolit SHA, capture the package version, .NET SDK, nearest-upward `global.json` runner configuration when present, the exact coverage command, and the selected test projects. For every selected project, run the same evaluated metadata query the CLI uses:

```text
dotnet msbuild <project> -getItem:PackageReference \
  -getProperty:TestingPlatformDotnetTestSupport,TargetFramework,TargetFrameworks \
  -property:Configuration=Release -nologo
```

Normalize property/package values with Unicode trimming and ASCII case-insensitive comparison. The only accepted normalized `global.json` runner values are `VSTest` and `Microsoft.Testing.Platform`, matching the [.NET runner-selection contract](https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet-test); missing `global.json` or an omitted `test.runner` maps to VSTest. Any malformed or other runner value is invalid. Evaluate once for a single target framework or once per TFM when `TargetFrameworks` is nonempty; a conflicting per-TFM result is `invalid`, not an averaged answer. For each TFM, `TestingPlatformDotnetTestSupport=true` requires MTP, `false` or unset permits VSTest, and any other value is invalid.

Read only evaluated direct `PackageReference.Identity` values, so conditional `Include` or `Update` entries and central package management are resolved by MSBuild before classification; transitive packages never count. The exact relevant identities are `coverlet.collector` for `collector`, `coverlet.msbuild` for `MSBuild`, and `coverlet.MTP` for `native`; package versions are recorded but do not alter identity. Aliases and original spelling do not create another identity after evaluation. Each TFM must expose exactly one relevant direct identity; zero or multiple identities are missing or conflicting. A global runner/property contradiction or engine/integration mismatch is invalid. Persist the normalized facts, then derive labels such as `VSTest / collector`, `VSTest / MSBuild`, or `MTP / native`; no classification is guessed.

Run exactly one current classified workflow. Attempt at most one historical preview.4 comparison only when the recorded consumer SHA and package inputs are already available; unavailable historical inputs are recorded as unavailable and trigger no retrieval work. Preview.4 explains the issue report but is not the acceptance test. For each attempted run retain bounded, sanitized evidence of the raw per-project report, merged report, gate result, and the `Skoolit.Web` assembly’s line and branch hit totals.

Outcome routing is explicit:

- Current VSTest collector proof contains real raw and merged hits: document the fixed-version boundary and close #674 with the evidence.
- Current VSTest raw report lacks `Skoolit.Web` or has zero hits: inspect collection, inclusion, engine classification, normalization, and expected report identity before changing merge logic.
- Current VSTest raw report has hits but the merged report loses them: investigate `CoverageMerge` fan-in with a focused regression.
- The consumer is MTP: document the engine mismatch or create a separate MTP-scoped issue. Do not quietly alter the VSTest collector contract.
- Engine or integration classification is invalid, unknown, missing, conflicting, or mismatched: block #674 on classification evidence. Draw no collector-regression conclusion and do not authorize a code, merge, or threshold change from that run.
- The required current checkout, restore, or classified workflow cannot run: record #674 as blocked on current external evidence. Do not close the issue or claim a fixed-version boundary; the owned package-proof hardening can still proceed.

### 2. Delivery boundary

The mandatory delivery is the semantic default-collector guard, its focused unit tests, the package-verification run, and the documentation/release evidence that describes that guard. Skoolit evidence determines only #674’s issue disposition and never gates package publication. A coverage-run or merge change is contingent on one demonstrated fault mechanism from the single current classified run; otherwise, no AppSurface runtime or CLI behavior changes.

### 3. Make the packed consumer proof semantic

In [CoverageCliConsumerProof.cs](../../tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs), keep the generated `Smoke` class library and `Smoke.Tests` direct reference. The existing fixture executes both `Calculator.Add` and both outcomes of `Calculator.Sign`; `Calculator.Untested` remains deliberately unexecuted for patch-target coverage.

The default proof selects exactly one VSTest project: `Smoke.Tests`; `Smoke.Msbuild.Tests` and the failing `Smoke.Browser.Tests` sentinel are excluded. `coverage run` must write a per-project `coverage-project.json` beside every project report with schema version, normalized solution-relative test-project path, and allocated slug. The PackageIndex proof enumerates only immediate `projects/*/coverage-project.json` manifests, validates their bounded schema and normalized relative paths, requires exactly one manifest for `Smoke.Tests/Smoke.Tests.csproj`, and then accepts only that manifest directory’s sibling `coverage.cobertura.xml`. It never calls the CLI-private slug allocator, guesses from a directory name, or recursively discovers arbitrary coverage files. An absent, nonregular, malformed, or duplicate matching manifest/report is a distinct failure. The merged report is exactly `TestResults/coverage-merged/coverage.cobertura.xml`; it must be one regular file. The fixture pins `coverlet.collector` 10.0.1, runs in the command’s default Debug configuration, and `Calculator.Sign` is source line 7 of the generated `Smoke/Calculator.cs` file. Do not infer correctness from an XML file’s existence, a report-level percentage alone, or test-process exit code.

Before finalizing parser assertions, run the existing generated fixture once in an isolated local proof directory and preserve the bounded raw and merged `Smoke.Calculator` XML fragments as test input. The supported subset below must match that observed collector/ReportGenerator shape. If the observed supported version differs, update the exact expected structural contract before implementation proceeds; do not silently broaden acceptance. This local contract-capture step is part of the owned fixture work, not an external release dependency.

Parse each file with `XmlReaderSettings` that sets `DtdProcessing=Prohibit` and `XmlResolver=null`, wrapped in a character-bounded reader that permits at most 1,048,576 input characters; enforce a maximum element depth of 32 and element count of 10,000 in the reader loop. The regular `coverage-project.json` manifest is separately capped at 16 KiB and is rejected when missing, nonregular, oversized, malformed, or ambiguous. Accept the supported Coverlet/ReportGenerator subset only: `coverage`, `sources`, `source`, `packages`, `package`, `classes`, `class`, `methods`, `method`, `lines`, `line`, `conditions`, and `condition`. Apply the documented per-element attribute allowlist strictly; reject unsupported attributes or shapes, DTDs, entities, missing required identity attributes, or invalid numeric fields with an `unsupported Cobertura schema` diagnostic.

The semantic validator must require, in both reports:

- exactly one `package` named `Smoke`, exactly one `class` named `Smoke.Calculator` beneath it, and a normalized `filename` ending in `Smoke/Calculator.cs` after the fixture’s explicit `[Smoke]*` inclusion;
- at least one executed `Calculator` line with `hits > 0`, plus exactly one line numbered `7` for `Calculator.Sign` with `hits > 0`;
- `Sign` line 7 with `branch="True"`, `condition-coverage="100% (2/2)"`, and exactly one child `condition` with `type="jump"` and `coverage="100%"`. The positive and negative test inputs must cover both ternary outcomes; one covered branch is not sufficient;
- no duplicate expected package, class, line number, or `condition` identity. Any missing, duplicate, conflicting, malformed, or all-zero datum is a semantic failure rather than evidence that an arbitrary report is acceptable;
- ASCII decimal numeric grammar only: positive `line/@number` and `condition/@number` values are `^[1-9][0-9]*$`; nonnegative `line/@hits` is `^(0|[1-9][0-9]*)$`; the supported `Sign` branch value is exactly `100% (2/2)`, agreeing with one `jump` condition at `100%`. Overflow, signs, whitespace, decimals where an integer is required, or inconsistent branch totals are schema failures;
- fatal document-shape failures (`CPV004`) stop semantic extraction only for that report, while the other report is still independently checked. After a report parses, collect all semantic failures in stable order: package, class, filename, line/hits, branch, duplicate identity. The final failure order is raw document/semantics followed by merged document/semantics;
- stable proof invariant identifiers and fields: `CPV001` selected raw manifest/report missing or nonregular, `CPV002` selected raw manifest/report malformed or ambiguous, `CPV003` merged report missing/nonregular, `CPV004` unsupported Cobertura schema, `CPV005` package identity, `CPV006` class identity, `CPV007` filename, `CPV008` `Sign` line/hits, `CPV009` `Sign` branch, `CPV010` duplicate identity, and `CPV011` merge loss. Each rendered failure includes `scope=raw|merged`, ordered `invariants`, and one artifact-relative path;
- report-specific failure wording that says whether the failed invariant was in the raw project report or merged report, and names only the fixture path and stable artifact-relative path.

Keep the existing default collector artifact checks, explicit MSBuild compatibility artifact checks, merge artifacts, passing and failing gate artifacts, patch-target proof, excluded sentinel, and canary proof. The new semantic assertions are mandatory for the default collector path and the merged output derived from that path; they do not claim equivalent MSBuild or MTP semantics without separate evidence.

Normalize a `class/@filename` by rejecting empty/control-character values, replacing `\\` with `/`, splitting on `/`, rejecting empty, `.` and `..` components, and comparing the final two components ASCII case-insensitively to `Smoke/Calculator.cs`. Earlier absolute-path or source-root components are not opened or rendered and may differ by host.

### 4. Preserve release and test seams

Place the focused semantic parse/validation helper with the consumer-proof workflow or an internal, narrowly named companion type. It is package-validation infrastructure, not public CLI API. It returns structured, deterministic result data that the existing Markdown report renderer can expose without absolute paths or raw XML.

Add unit coverage in [PackageArtifactValidationTests.cs](../../tools/ForgeTrust.AppSurface.PackageIndex.Tests/PackageArtifactValidationTests.cs) for successful raw-plus-merged evidence and every failure category: missing or ambiguous report, prohibited DTD/entity, oversized/deep document, incorrect root or unsupported element shape, absent or duplicate expected package/class/line/condition, invalid filename, zero line hits, incomplete `Sign` branch representation, and a merge-only loss of otherwise valid raw hits. Reuse existing test doubles for command execution; no network, package publication, or real Skoolit checkout belongs in this unit test matrix.

If the single classified current VSTest reproduction demonstrates one fault outside the proof contract, add the smallest focused regression only for that demonstrated mechanism in the relevant coverage-run or merge suite. Do not change [CoverageRunDrivers.cs](../../Cli/ForgeTrust.AppSurface.Cli/CoverageRunDrivers.cs), scheduling, watchdog behavior, output ownership, or engine support for unrelated reasons.

### 5. Document the verification boundary

Update the package-proof reference in [packages/README.md](../../packages/README.md), the AppSurface CLI coverage documentation when behavior changes there, and the coordinated release notes. Explain that the pre-publish consumer verifies nonzero raw and merged coverage for an owned, directly exercised assembly; it does not certify arbitrary external application graphs. Link readers to the existing coverage-run driver-selection guidance for the VSTest/MTP boundary.

## Success Criteria

1. The Skoolit evidence bundle classifies test engine and coverage integration independently before coverage execution and derives a non-ambiguous current workflow label, or records current external evidence as blocked without closing #674.
2. The current package graph, and at most one available preview.4 comparison, report raw and merged `Skoolit.Web` hit totals without publishing source, credentials, or unbounded logs. This evidence decides issue disposition only.
3. The packed CLI consumer proof selects exactly one manifest-bound `Smoke.Tests` canonical report, fails before publication for `CPV001` through `CPV011` as applicable, and requires the exact covered `Smoke.Calculator` line-7 branch invariant in both raw and merged reports.
4. A raw report that satisfies every semantic invariant but a merged report that loses `Smoke.Calculator`, line-7 hits, or its `100% (2/2)` branch representation fails as `CPV011`.
5. Valid default-collector raw and merged reports pass, and the existing explicit MSBuild artifact proof, excluded sentinel, passing/failing gates, patch-target check, and canary proof still pass unchanged.
6. The package verification workflow and release artifact upload retain the structured proof report, including scope, invariant IDs, and artifact-relative paths, so the publish decision has inspectable evidence without Skoolit access.

## Distribution Plan

No new package or runtime artifact is introduced. The existing `ForgeTrust.AppSurface.Cli` .NET tool package and `verify-packages` workflow remain the distribution and release gates. GitHub Actions already publishes the consumer-proof report and coverage artifacts; the strengthened proof becomes part of that existing pre-publish evidence.

## Dependencies

- Access to the recorded Skoolit SHA and its current package restore path for the external evidence run. If unavailable, #674 remains blocked on that evidence but the owned fixture work remains releasable.
- Availability of the preview.4 package only for historical comparison; absence does not block current-version verification or the permanent fixture guard.
- The established #672 collector-default architecture and its current driver/normalization tests.

## Next Steps

1. Capture the supported raw and merged Cobertura shape from the generated default-collector fixture and freeze it as test input.
2. Add the internal semantic Cobertura validator and structured proof results to the packaged consumer workflow.
3. Add the focused package-index tests for positive, malformed, missing, ambiguous, zero-hit, branch, duplicate, and merge-loss outcomes.
4. Run one current classified Skoolit workflow and make at most one historical preview.4 comparison attempt when inputs are already available; record raw-versus-merged hit totals.
5. Update verification documentation and release notes, run package verification, then close #674, mark it blocked on external evidence, or create a narrowly evidenced follow-up based on the current run.

## The Assignment

Run the current Skoolit workflow once with engine/integration classification captured before the coverage command. Bring back only this bounded evidence: SDK, package version, `global.json` runner selection, evaluated `TestingPlatformDotnetTestSupport`, direct coverage package, raw `Skoolit.Web` line/branch hit totals, merged totals, and gate result. That one record decides whether we build a code fix or close #674 with a fixed-version boundary.

## What I noticed about how you think

- You chose the public-maintenance goal over a Skoolit-only workaround, which keeps the package contract ahead of a one-off passing build.
- You selected dual proof, then strengthened it with runner classification when the independent review found the MTP edge case. That turns an assumption into an observed fact.
- You chose the existing package proof instead of a new CLI option. The result is a durable release guarantee without creating API surface that no adopter has requested.

## Autoplan Phase 1: CEO Review

### Scope, premise, and mode

The human premise gate selected **A** on 2026-08-18. The package release requirement is an owned, semantic proof that the known `Smoke.Tests` consumer exercises `Smoke.Calculator` and retains that evidence from its canonical raw report through the merged report. A zero-hit external consumer report becomes a collection or merge defect only after the run also proves target-assembly execution, identity, symbols/source identity, and include/exclude filtering. This avoids treating an ordinary consumer configuration error as a CLI regression.

The review mode is **SELECTIVE EXPANSION**. The core proof remains the delivery boundary. Adjacent improvements are admitted only when they remove a release-proof blind spot without creating public CLI API or changing the coverage engine contract.

### 0A. Premise challenge

| Premise | Verdict | Evidence and decision |
| --- | --- | --- |
| File existence is not meaningful coverage evidence. | Confirmed | The current package consumer proof can accept placeholder Cobertura XML and selects a discovered report. A semantic class, line, and branch assertion is required. |
| A zero-hit external report proves a CLI defect. | Corrected | Raw-before-merge diagnosis is valid, but external execution, assembly/PDB identity, filters, engine, and integration must be recorded before attributing failure to the CLI. |
| The default collector path is the #674 release surface. | Confirmed | `CoverageRunCommand` defaults to `collector`; the packed fixture directly references `Smoke` from `Smoke.Tests`. Explicit MSBuild remains an artifact-compatibility path unless separate semantic evidence warrants more. |
| The external current graph is a release gate. | Rejected | It decides the issue disposition only. Package publication cannot depend on a third-party checkout or feed state. |

### 0B. What already exists

| Sub-problem | Existing owner | Reuse decision |
| --- | --- | --- |
| Disposable package-consumer setup and command transcript | `CoverageCliConsumerProofWorkflow` | Extend rather than create another release workflow. |
| Per-project raw-artifact layout, merge output, and safe path checks | `CoverageRun` and `CoverageRunArtifactReader` | Derive the expected `Smoke.Tests` project slug with the existing allocation rule; do not rediscover by broad file search. |
| VSTest collector/MSBuild capability handling | `CoverageRunDrivers` | Preserve the existing CLI contract. The external classifier must either be explicitly diagnostic-only or share a tested contract. |
| Command and artifact seam | `PackageArtifactValidationTests` recording runner | Add deterministic Cobertura fixtures and negative cases, not network or external-repository tests. |
| Package verification and release evidence | PackageIndex `verify-packages` path | Publish a scrubbed semantic summary; retain full command logs only in private artifacts. |

### 0C. Dream-state delta

```text
CURRENT
  package proof proves commands exited and files appeared
        |
        v
THIS PLAN
  package proof proves its known consumer executed a class and branch in
  exactly one raw project report, then preserved that fact in merged output
        |
        v
12-MONTH IDEAL
  deterministic, privacy-safe semantic coverage evidence catches collection,
  identity, and fan-in regressions while driver-specific boundaries remain clear
```

The plan advances toward the ideal without promising that one fixture certifies arbitrary consumer graphs. The remaining gap is intentionally recorded as diagnostic evidence and future graph-shape coverage rather than release-blocking scope.

### 0C-bis. Implementation alternatives

| Approach | Effort | Risk | Pros | Cons | Decision |
| --- | --- | --- | --- | --- | --- |
| A. Canonical raw-report semantic sentinel plus raw-to-merged conservation | M | Low | Directly catches the known false-positive class; reuses the existing package proof; remains package-owned and deterministic. | Requires a bounded Cobertura parser/test fixture contract. | **Selected.** |
| B. Evidence-only external Skoolit rerun | S | High | Smallest code diff; may explain the historical report. | Cannot prevent release-time recurrence; depends on external source and feed state. | Rejected. |
| C. Full generalized report-model platform now | XL | Medium | Could support arbitrary graph diagnostics and every driver. | Premature public/internal abstraction, broad migration risk, and not needed to prove #674. | Deferred. |

The selected approach is complete for the owned release claim. It adds exact project identity and raw-to-merged conservation, not merely one report-level percentage check.

### 0D. Selective-expansion scan

The mandatory change touches the consumer-proof workflow, its tests, package/release evidence documentation, and possibly the CLI coverage documentation. It introduces no public class or new runtime service. The following candidates were evaluated:

| Candidate | Decision | Rationale |
| --- | --- | --- |
| Exact `Smoke.Tests` identity controls both validation and copied merge shard. | Accepted in P0 | A valid unrelated report must never satisfy the selected consumer proof. |
| Raw-to-merged positive-hit conservation for the selected class/line/branch. | Accepted in P0 | It directly detects merge loss and complements the fixture sentinel. |
| Public, scrubbed semantic release summary. | P1 in this delivery | The report must not expose host paths, raw XML, credentials, or arbitrary command output. |
| Shared engine/integration classifier that changes CLI behavior. | Deferred | No current evidence proves the VSTest CLI contract is wrong; a diagnostic classifier must not silently redefine it. |
| Multi-project or multi-target synthetic graph proof. | Deferred to follow-up | Valuable regression breadth, but not necessary to close the owned release blind spot. |
| Public consumer-declared sentinel option. | Rejected | Expands API surface before adopter demand. |

### 0E. Temporal interrogation

| Implementation stage | Decision locked now |
| --- | --- |
| Hour 1: fixture and paths | Compute one canonical `Smoke.Tests` slug and make the raw report, copied shard, and merged input trace back to it. |
| Hour 2-3: parser and semantics | Freeze the observed generated-fixture subset, enforce document bounds, and evaluate raw and merged reports independently. |
| Hour 4-5: diagnostics | Emit invariant IDs, `raw` or `merged` scope, fixture-relative paths, and scrubbed bounded explanations. |
| Hour 6+: external evidence | Record SDK, exact package graph/assets fingerprint, package artifact digest, consumer SHA, runner, direct package identities, filters, execution/assembly/symbol facts, and report hit totals. |

### Sections 1-11

#### 1. Architecture

```text
packaged CLI
    |
    v
generated Smoke solution
    |
    +--> Smoke.Tests (collector, direct Smoke reference)
    |          |
    |          v
    |     canonical raw report
    |          |
    |          +--> semantic validator (raw scope)
    |          +--> exact selected shard copy
    |
    v
coverage merge --> merged report --> semantic validator (merged scope)
                                      |
                                      v
                             scrubbed package-proof summary
```

Happy path: the one selected raw report and merged report both satisfy the known `Smoke.Calculator` line-7 branch invariant. Missing path: absent/nonregular raw or merged report emits its scoped invariant before any broad discovery can compensate. Empty path: an XML document with no expected package/class/line is a semantic failure. Error path: unsupported or hostile XML is bounded, rejected, and reported without leaking content. The design has no persistent state machine, network service, endpoint, or authorization boundary. Rollback is a normal revert of the package-proof change; no migration or feature flag is needed.

#### 2. Error and rescue registry

| Codepath | Failure | Handling | User/release evidence |
| --- | --- | --- | --- |
| Canonical raw report resolution | Missing, nonregular, duplicate, or wrong-project path | Fail the proof with `CPV001` or `CPV002`; do not fall back to another project. | Release fails with scoped invariant and artifact-relative path. |
| Cobertura extraction | DTD/entity, oversized/deep XML, unsupported shape, malformed numeric field | Reject the affected report as `CPV004`; still inspect the other report independently. | Release fails with bounded schema diagnostic. |
| Semantic validation | Missing/duplicate package, class, line, condition, zero hits, or incomplete branch | Collect stable failures per report in deterministic order. | Release shows invariant IDs and `raw` or `merged` scope. |
| Raw-to-merged comparison | Valid raw evidence missing or weakened after merge | Fail `CPV011`; preserve the raw success evidence. | Release identifies merge loss, not collection loss. |
| External consumer diagnosis | Checkout, restore, classification, identity, or execution fact absent | Mark #674 external evidence blocked/invalid; do not change CLI behavior or close the issue. | Issue record states what was not proven. |

No catch-all success path is permitted. Command logs remain private diagnostics; release-facing rendering contains only scrubbed structured evidence.

#### 3. Security and threat model

| Threat | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- |
| XML entity expansion or oversized document | Medium | High | Prohibit DTDs, null resolver, 1 MiB character limit, depth 32, node cap 10,000. |
| Path traversal or symlink substitution | Medium | High | Resolve expected artifact-relative paths through existing guarded path ownership; require regular files and never open source paths from XML. |
| Host path, credentials, or XML leakage in release output | Medium | High | Add a scrubbed public mode with fixture-relative paths and bounded invariant messages; test redaction. |
| Malicious report ambiguity hides a failing target | Medium | High | Require exactly one canonical selected report and reject duplicates instead of first-match selection. |

There are no new secrets, endpoints, data stores, or runtime dependencies. The only new untrusted input is Cobertura XML already produced within the proof directory.

#### 4. Data-flow and interaction edge cases

```text
expected project identity -> guarded raw path -> parse -> semantic facts -> copy selected shard
          |                       |             |             |                  |
          v                       v             v             v                  v
     invalid slug            missing/ambig    DTD/shape      zero/duplicate      no fallback
          |                       |             |             |                  |
          +-----------------------+-------------+-------------+------------------+
                                                  |
                                                  v
                                           scoped proof failure
```

The plan handles nil/missing, empty, malformed, duplicate, and merge-only-loss paths. There is no interactive UI. The important edge case is a valid unrelated report beside a missing or zero-hit `Smoke.Tests` report; it must fail before copying any shard.

#### 5. Code quality

The helper stays internal and narrowly scoped to `CoverageCliConsumerProofWorkflow` or a companion type. It must share one canonical-report selection result between raw validation and shard copying to prevent duplicated selection logic. Parsing, semantic extraction, and rendering are separated so the renderer cannot reinterpret untrusted XML. A generalized report abstraction is intentionally deferred because the only owned contract is the generated Coverlet/ReportGenerator subset.

#### 6. Test review

```text
fixture creation
  -> canonical slug allocation
     -> raw report resolution: success / missing / duplicate / unrelated valid report
     -> schema parsing: valid / DTD / size / depth / shape / numeric failure
     -> semantics: package / class / filename / Sign line / branch / duplicate identity
     -> merge retention: raw valid + merged valid / raw valid + merged loss
     -> renderer: scoped IDs retained; absolute paths, XML, credentials absent
```

The shipping-at-2am test is a valid raw selected report whose merged report loses only `Smoke.Calculator` line-7 branch evidence and therefore fails `CPV011`. Hostile tests cover an unrelated valid report, duplicate expected report, DTD, path-like filename, zero hits, partial branch, and unsafe text in release evidence. Tests use static fixture XML and the existing recording command runner, so they are deterministic and do not depend on external package feeds, time, or test ordering.

#### 7. Performance

The proof parses at most two bounded XML reports and walks at most 10,000 elements per report. It must stream rather than materialize arbitrary XML and must not recursively scan the proof directory beyond the canonical expected path. This is negligible compared with the existing package-install and `dotnet test` work. A malformed large report fails within configured parser bounds rather than consuming unbounded memory.

#### 8. Observability and debuggability

The summary records proof result, package version/artifact digest, driver boundary, `raw`/`merged` scope, invariant IDs, and artifact-relative paths. Private artifacts retain command logs for maintainers. The issue evidence bundle additionally records consumer SHA, assets/lock fingerprint, SDK, runner, direct coverage identities, filters, target assembly/PDB facts, and report hit totals. This makes a later false-zero report diagnosable without publishing consumer source or machine details.

#### 9. Deployment and rollout

No database migration, environment parity change, or runtime rollout occurs. The delivery sequence is: capture the known fixture report shape; add deterministic parser/semantic tests; extend the consumer proof; run package verification; inspect scrubbed proof evidence; publish through the existing gate. If the proof unexpectedly rejects current valid output, retain the captured artifact privately, adjust the explicitly versioned supported subset, and rerun. Rollback is a git revert before package publication.

#### 10. Long-term trajectory

Reversibility is **5/5** because the change is release-validation infrastructure with no public API. The main debt avoided is a misleading green package release. Deferred work is a broader synthetic graph and, only with evidence, a shared engine classifier or wider semantic proof for MSBuild/MTP. The plan is understandable in twelve months because invariant names, fixture identity, and raw-to-merged boundary are written into the proof contract.

#### 11. Design and UX

Skipped. The plan has no UI, screen, component, or end-user interaction surface. The only reader-facing surface is concise package/release evidence, reviewed under observability and DX.

### Failure modes registry

| Codepath | Failure mode | Rescued? | Test? | User sees | Logged? |
| --- | --- | --- | --- | --- | --- |
| Raw selection | Different project report is selected | Yes, fail closed | Yes | `CPV001`/`CPV002` | Structured private + public-safe summary |
| XML parser | Hostile or unsupported report accepted | Yes, fail closed | Yes | `CPV004` | Structured private + public-safe summary |
| Semantic facts | Zero or partial coverage passes | Yes, fail closed | Yes | `CPV005`-`CPV010` | Structured private + public-safe summary |
| Merge | Raw proof exists but merged report loses it | Yes, fail closed | Yes | `CPV011` | Structured private + public-safe summary |
| External diagnosis | Consumer facts are missing | Yes, issue blocked, no conclusion | Yes, protocol-level | Bounded issue evidence | Issue record |

There are no silent or untested critical gaps in the mandatory path.

### Not in scope

- A new public CLI option for consumer-declared sentinels. The owned fixture meets the release proof without new API.
- Changing `CoverageRunDrivers` or adding MTP coverage support. This requires a demonstrated compatibility fault, not a historical report alone.
- Making Skoolit checkout, restore, or historical preview.4 availability a package publication prerequisite.
- A generalized Cobertura report model or arbitrary consumer-graph certification.
- A synthetic multi-target/multi-assembly graph proof. This is valuable follow-up work, not required for the #674 invariant.

### Independent voice and consensus

`combo/sub` reviewed the plan independently. The separate Codex CLI voice is unavailable in this environment because its local state database cannot be opened read-write; no alternate model was substituted.

| Dimension | Subagent conclusion | Primary review conclusion | Consensus |
| --- | --- | --- | --- |
| Premises valid | Correct raw-before-merge idea, but external zero-hit attribution needs execution/identity/filter evidence. | Same. | Confirmed |
| Right problem | Exact artifact selection must govern validation and shard copy. | Same. | Confirmed |
| Scope | Stage broader hardening after the core semantic guard. | Same. | Confirmed |
| Alternatives | Add raw-to-merged conservation before a generalized parser platform. | Same. | Confirmed |
| Release risk | Existing renderer may expose unsafe diagnostics. | Same. | Confirmed |
| Temporal decision | Current graph needs immutable evidence fingerprint. | Same. | Confirmed |

### Decision Audit Trail

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
| --- | --- | --- | --- | --- | --- |
| 1 | CEO | Confirm corrected release-proof premise | Human gate | User sovereignty | User selected A: prove owned raw-to-merged semantics; external attribution requires observed execution and identity facts. | Broad unqualified tool-defect premise |
| 2 | CEO | Canonicalize `Smoke.Tests` report identity for validation and shard copy | Mechanical | P1 completeness | Prevents a valid unrelated report from satisfying the proof. | First-discovered report selection |
| 3 | CEO | Add raw-to-merged conservation to the fixture sentinel | Mechanical | P1 completeness | Distinguishes collection loss from merge loss with the owned consumer. | File-existence or aggregate-only validation |
| 4 | CEO | Keep current CLI driver behavior unchanged | Mechanical | P5 simplicity | No demonstrated engine implementation defect justifies changing VSTest/MTP behavior. | Incidental driver rewrite |
| 5 | CEO | Label MSBuild assurance as artifact compatibility, not semantic equivalence | Mechanical | P6 evidence | Avoids overstating what the proof verifies. | Implicit broad compatibility claim |
| 6 | CEO | Add scrubbed release-facing semantic evidence | Mechanical | P1 completeness | A release gate must be inspectable without leaking host paths, raw XML, or command output. | Reusing the full private renderer as public evidence |
| 7 | CEO | Stage graph-shape fixture and generalized classifier work | Mechanical | P3 outside-blast-radius deferral | They improve future coverage but do not block the known #674 release invariant. | Expanding this issue into a platform rewrite |
| 8 | CEO | Fingerprint external evidence inputs | Mechanical | P6 reproducibility | Consumer SHA, assets/lock hash, package digest, SDK, runner, filters, and identity facts make the current baseline repeatable. | Time-only baseline record |
| 9 | CEO | Bind selected raw report to copied merge shard by SHA-256 | Mechanical | P1 completeness | Path reuse alone cannot prove that the merge consumed unchanged selected evidence. | Path-only provenance |
| 10 | CEO | Introduce a separate versioned scrubbed evidence artifact | Mechanical | P6 compatibility | Preserves private maintainer report consumers while making public release evidence safe. | Reformatting or redacting the existing private report in place |
| 11 | Eng | Emit manifest-bound project identity from `coverage run` | Mechanical | P4 reuse without wrong-direction coupling | The CLI owns its private slug allocator; its output must bind that slug to a normalized project path instead of duplicating or referencing it from PackageIndex. | PackageIndex-to-CLI reference or copied hash algorithm |
| 12 | Eng | Select only one immediate matching manifest and regular sibling report | Mechanical | P1 completeness | Replaces both current first-match search paths and rejects unrelated, malformed, duplicate, directory, and symlink evidence. | Recursive coverage-file discovery |
| 13 | Eng | Preserve existing detailed renderer and add separate scrubbed artifact | Mechanical | P6 compatibility | Existing consumers retain private diagnostics while release evidence becomes safe and bounded. | In-place renderer redaction |
| 14 | Eng | Use captured bounded XML fixtures plus one test path per CPV invariant | Mechanical | P1 completeness | The current `<coverage />` test double proves existence only and cannot validate semantics. | Mock-only or report-level aggregate tests |
| 15 | Eng | Keep external classification as diagnostic-only owned record | Mechanical | P3 scope boundary | The current evidence decides #674 disposition but must not join the package publication path. | Automating third-party checkout as package-gate test |
| 16 | DX | Document local readiness and packaged semantic proof as distinct proof levels | Mechanical | P1 complete developer path | Maintainers need to know which command validates their repository versus the packed CLI release contract. | One ambiguous coverage workflow description |
| 17 | DX | Make every CPV result self-contained and actionable | Mechanical | P1 fight uncertainty | Stable codes need scope, cause, next action, and safe evidence location, not only an opaque failure string. | Private command-ledger debugging |
| 18 | DX | Surface runner, integration, and assurance level in safe evidence | Mechanical | P6 evidence boundary | Driver choice changes what the proof means; MSBuild and MTP must not be inferred from command text. | Implicit or overbroad compatibility claim |
| 19 | DX | Fix the scrubbed evidence filename and schema policy | Mechanical | P6 compatibility | A versioned stable artifact gives CI and readers a safe contract while private Markdown remains unchanged. | Unnamed ad hoc release output |
| 20 | DX | Publish one driver/escape-hatch decision tree and upgrade procedure | Mechanical | P5 simplicity | Existing docs are thorough but distributed; a short decision path reduces incorrect driver switches and support triage. | Forcing readers to infer assurance from multiple sections |

### CEO completion summary

```text
+====================================================================+
| MEGA PLAN REVIEW — CEO COMPLETION SUMMARY                          |
+====================================================================+
| Mode selected        | SELECTIVE EXPANSION                         |
| System audit         | Existing package proof has the correct seam  |
| Step 0               | Corrected premise; minimal semantic guard     |
| Architecture          | 3 material refinements accepted                |
| Error/security/data   | 5 fail-closed paths mapped                     |
| Code quality/tests    | Shared identity selection and deterministic XML |
| Performance           | Two bounded streaming parses                   |
| Observability/deploy  | Scrubbed evidence; existing package gate       |
| Long-term trajectory  | Reversibility 5/5; broad graph work deferred   |
| Design/UX             | SKIPPED: no UI                                 |
+--------------------------------------------------------------------+
| NOT in scope         | 5 items documented                              |
| Failure modes        | 5 total, 0 silent critical gaps                 |
| Outside voice        | subagent ran; Codex unavailable                 |
| Consensus            | 6/6 dimensions confirmed                        |
| Unresolved decisions | 0                                                |
+====================================================================+
```

### CEO implementation tasks

- [x] **T1 (P1, human: ~2h / CC: ~15min)** — Package consumer proof — make the selected `Smoke.Tests` raw report the single source for semantic validation and merge-shard copy.
  - Surfaced by: CEO architecture and independent voice finding F2.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs`, `tools/ForgeTrust.AppSurface.PackageIndex.Tests/PackageArtifactValidationTests.cs`.
  - Verify: selected report succeeds; missing, duplicate, and unrelated-valid-report fixtures fail deterministically.
- [x] **T2 (P1, human: ~3h / CC: ~25min)** — Cobertura semantic proof — validate the owned `Smoke.Calculator` raw and merged line-7 branch invariant plus merge-loss failure.
  - Surfaced by: CEO test and data-flow review.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs`, `tools/ForgeTrust.AppSurface.PackageIndex.Tests/PackageArtifactValidationTests.cs`.
  - Verify: package-index tests cover valid, malformed, zero-hit, partial-branch, and raw-valid/merged-loss cases.
- [x] **T3 (P2, human: ~1h / CC: ~10min)** — Release evidence — render only scrubbed semantic fields and document the collector/MSBuild assurance boundary.
  - Surfaced by: CEO security and observability review.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs`, `packages/README.md`, `Cli/ForgeTrust.AppSurface.Cli/README.md`, release notes.
  - Verify: renderer tests reject absolute paths, credentials, raw XML, and unbounded command excerpts.

### CEO adversarial-review refinements

Three independent adversarial rounds strengthened the delivery contract. The selected canonical raw report and its copied merge shard must carry equal SHA-256 digests. Every `CPV001` through `CPV011` result must be traceable to a scoped public-safe evidence record and a deterministic test case. The existing detailed consumer-proof Markdown remains a private maintainer artifact; release upload adds a separate versioned scrubbed semantic-evidence artifact rather than changing existing report fields. The external Skoolit run remains non-gating: it records a timestamped current package graph and determines only whether #674 closes, blocks on evidence, or yields a focused follow-up.

## Autoplan Phase 3: Engineering Review

### Step 0. Scope challenge and current-code evidence

The change is accepted at its current boundary. It affects the existing CLI coverage-output contract, PackageIndex consumer proof, focused CLI/package-index tests, and documentation/release evidence. That is five logical surfaces, not a new service or package. Reducing it would leave either the identity, semantics, provenance, or privacy failure class unaddressed.

The directly verified existing behavior explains the failure mode:

- `CoverageCliConsumerProof.CheckGlob` returns the first matching `projects/*/coverage.cobertura.xml` path, and `CopyCoverageShard` takes the first sorted recursive Cobertura file. Neither proves it belongs to `Smoke.Tests`.
- `CheckArtifact` checks only `File.Exists`, and the recording test runner writes `<coverage />` placeholders. The current proof cannot distinguish a valid report from an empty schema shell.
- `CoverageRun.AllocateProjectSlugs` is private to the CLI project, so PackageIndex cannot safely invoke it without introducing a dependency in the wrong direction.
- The detailed proof renderer emits work, NuGet, artifact, log, command, and output paths. It is a private diagnostic, not safe release evidence.

The engineering decision is a bounded `coverage-project.json` manifest emitted by the CLI next to every per-project report. This preserves the CLI as owner of its allocation rule while giving PackageIndex a stable, dependency-free identity contract. No coverage-driver, scheduler, watchdog, or MTP behavior changes.

### 1. Architecture review

```text
CoverageRun project execution
    |
    v
projects/<allocated-slug>/
    |- coverage-project.json  { schemaVersion, relativeProjectPath, slug }
    |- coverage.cobertura.xml
    |- dotnet-test.log
    |
    v
PackageIndex consumer proof
    |- enumerate immediate manifests only
    |- choose exactly one Smoke.Tests/Smoke.Tests.csproj manifest
    |- require regular sibling raw Cobertura report
    |- hash + validate raw semantics
    |- copy the selected report and compare raw/shard hashes
    v
coverage merge -> merged Cobertura -> independent semantic validation
    |
    +--> private detailed report (existing compatibility)
    +--> versioned scrubbed release evidence (new)
```

The manifest is a narrow output-contract addition, not a new public command. Its parser accepts a fixed schema version and relative project path only; it rejects unsafe values, a mismatched slug/directory, zero/multiple target matches, a nonregular report, and recursive fallback. The selected artifact object carries path, fixture-relative path, manifest facts, SHA-256, raw semantics, and copied-shard SHA-256 to every downstream operation. `CPV011` compares raw and merged semantic facts, not bytes, because ReportGenerator legitimately changes report structure.

The only new production failure is an older/invalid CLI package missing the manifest. It must fail `CPV001` before merge, which is appropriate because the proof is testing the candidate packed CLI. Rollback is a revert before publication. There is no migration, endpoint, persisted state, or runtime deployment concern.

### 2. Code-quality review

The plan avoids a wrong-direction PackageIndex-to-CLI reference and avoids duplicated slug hashing. `CoverageRun` owns allocation and manifest emission; PackageIndex owns consumption and proof semantics. The manifest selection, regular-file validation, hash calculation, XML parse result, and renderer model should be internal types with one immutable selected-artifact value passed to validation and copy operations. No broad `Directory.EnumerateFiles(..., AllDirectories)` remains on the canonical proof path.

The existing private full report remains byte-compatible for maintainer diagnostics. The new scrubbed evidence should be a separate renderer and versioned model, which prevents a security fix from silently changing downstream consumers of the detailed report. The external Skoolit investigation is owned by the #674 implementer as a diagnostic-only record with a bounded JSON schema; it is not invoked by the package consumer workflow.

### 3. Test review

```text
CLI output contract                                  Package consumer proof
CoverageRun writes manifest                          package selects target manifest
  |- valid relative path [unit]                        |- one matching target [unit]
  |- invalid/unsafe path rejected [unit]               |- missing/malformed/duplicate [unit]
  |- slug/directory mismatch rejected [unit]            |- sibling regular-file check [unit]
          |                                                     |
          +------------------------+----------------------------+
                                   |
                                   v
raw semantic report [unit fixtures]
  |- valid Smoke.Calculator / line 7 / 2 branches
  |- DTD/entity, expansion, over-size, over-depth, bad shape
  |- package/class/filename/numeric/duplicate/zero/partial failures
                                   |
                                   v
selected raw hash == copied-shard hash [integration seam]
                                   |
                                   v
merged semantic report [unit fixtures]
  |- valid retention
  |- raw valid + merged loss => CPV011
                                   |
                                   v
release evidence [renderer tests]
  |- allowed relative identifiers only
  |- no root paths, credentials, raw XML, commands, output excerpts
```

The current recording runner must stop creating only `<coverage />` files for successful semantic paths. It needs to use fixed, bounded raw and merged Cobertura samples captured from one isolated generated fixture run. Existing command-sequencing, excluded sentinel, explicit MSBuild artifact, gate, patch-target, and canary tests remain. New tests cover each `CPV001` through `CPV011`, including an unrelated valid project report, so a real false-positive cannot regress silently.

### 4. Performance and operations review

The CLI writes one tiny JSON manifest per executed project. PackageIndex reads immediate manifests only, streams at most two one-megabyte XML files, hashes raw plus copied shard once, and performs no network access beyond the existing package proof. The 10,000-element and depth-32 limits prevent parser resource escalation. No cache, database, queue, metric backend, or alert is required.

Private logs remain the maintainer troubleshooting path. The public-safe artifact answers the release question in one line: which driver assurance was proved, which selected relative report was inspected, whether raw/merged semantics passed, and which invariant failed. MSBuild output must be explicitly labelled `artifact-compatibility`; collector output is labelled `semantic raw-and-merged proof`.

### Engineering failure modes

| Codepath | Realistic failure | Error handling | Test | Release result |
| --- | --- | --- | --- | --- |
| Manifest emission | Candidate CLI omits/misstates manifest | Fail selection before merge | Manifest unit tests | `CPV001` or `CPV002` raw scope |
| Manifest selection | Valid unrelated report or duplicate target report | Exactly-one normalized target match | Unrelated/duplicate fixtures | `CPV002` raw scope |
| File ownership | Directory, symlink, traversal, or nonregular report | Existing guarded semantics/equivalent fail-closed helper | File-kind/path fixtures | `CPV001` or `CPV002` raw scope |
| XML parsing | Malicious/unsupported report | Streaming bounded rejection | DTD/entity/depth/size/shape fixtures | `CPV004` |
| Provenance | Shard differs from validated raw report | Compare SHA-256 before merge | Digest-mismatch fixture | Scoped proof failure |
| Fan-in | Merge loses class, line, or branch fact | Independently validate merged report | Raw-valid/merged-loss fixture | `CPV011` merged scope |
| Public renderer | Private host data appears in release artifact | Separate allowlist-based renderer | Secret/path/XML/output rejection tests | Renderer test failure, no publish |

No row is silent or untested in the mandatory proof path.

### What already exists

- `CoverageRun` already allocates project directories, owns output markers, and performs coverage execution. It gains only a manifest emission/validation contract.
- `CoverageCliConsumerProofWorkflow` already creates the disposable consumer, invokes collector and MSBuild paths, copies a shard, merges, gates, and renders reports. It gains semantic selection/provenance, not another workflow.
- `CoverageRunArtifactReader` already demonstrates guarded coverage-artifact path rules. Reuse or mirror its fail-closed regular-file behavior rather than introducing ad hoc file handling.
- `PackageArtifactValidationTests` already owns the recording command runner. It gains deterministic XML/manifest fixtures rather than integration with external Skoolit.

### Not in scope

- A PackageIndex reference to the CLI assembly or a shared generic coverage platform. The manifest avoids that coupling.
- Semantic MSBuild or MTP proof without targeted driver evidence.
- Fetching, cloning, or publishing from the automated unit-test matrix.
- Changing existing private detailed-report fields or their consumers.
- General source-path normalization beyond the controlled fixture `Smoke/Calculator.cs` identity.

### Parallelization strategy

| Step | Modules touched | Depends on |
| --- | --- | --- |
| Emit and validate `coverage-project.json` | `Cli/ForgeTrust.AppSurface.Cli`, CLI tests | — |
| Select, hash, parse, and validate consumer artifacts | `tools/ForgeTrust.AppSurface.PackageIndex`, package-index tests | Manifest contract |
| Add scrubbed evidence and documentation | PackageIndex renderer, package/CLI docs, release notes | Structured semantic result |
| Run classified Skoolit evidence | External diagnostic workspace | Owned proof schema only |

Lane A is CLI manifest emission and its tests. Lane B is external diagnostic evidence protocol. After Lane A, Lane C implements PackageIndex semantic proof and renderer sequentially because they share the selected-artifact model. Documentation follows Lane C. There is no safe parallel edit lane inside PackageIndex itself.

### Independent voice and engineering consensus

The independent `combo/sub` engineering reviewer confirmed all material architecture findings. The Codex CLI voice is unavailable because the local state database cannot be opened read-write.

| Dimension | Subagent | Primary review | Consensus |
| --- | --- | --- | --- |
| Architecture | Existing seam is right; add semantics before merge/gate. | Same. | Confirmed |
| Coupling | CLI-private slug allocator is inaccessible. | Replace with emitted manifest, not a project reference. | Confirmed |
| Failure modes | First-match, nonregular, malformed, and merge-loss cases need fail-closed checks. | Same. | Confirmed |
| Test coverage | Placeholder XML cannot validate semantic contract. | Freeze bounded samples plus one case per invariant. | Confirmed |
| Security | Current renderer is private-only and file selection is permissive. | Separate scrubbed evidence and guarded selection. | Confirmed |
| Rollout | No runtime migration; publish after proof/evidence verification. | Same. | Confirmed |

### Engineering completion summary

```text
Step 0: scope accepted as-is; no reduction
Architecture Review: 2 design corrections, both folded
Code Quality Review: 2 ownership/compatibility corrections folded
Test Review: complete codepath and failure diagram produced; 11 invariant groups mapped
Performance Review: bounded two-report parse and two hashes; no new service load
NOT in scope: 5 items documented
Failure modes: 7 mapped, 0 silent critical gaps
Outside voice: subagent ran; Codex unavailable
Parallelization: 3 lanes, 2 initial parallel / PackageIndex lane sequential
Lake Score: 6/6 material decisions chose the complete option
```

### Engineering implementation tasks

- [x] **T4 (P1, human: ~2h / CC: ~15min)** — CLI coverage output — emit and validate a versioned per-project manifest binding allocated directory slug to normalized solution-relative project path.
  - Surfaced by: Engineering architecture review, verified private allocator coupling.
  - Files: `Cli/ForgeTrust.AppSurface.Cli/CoverageRun.cs`, `Cli/ForgeTrust.AppSurface.Cli.Tests/CoverageRunTests.cs`.
  - Verify: each selected test project emits one valid manifest; unsafe/mismatched values fail closed.
- [x] **T5 (P1, human: ~3h / CC: ~25min)** — Package consumer selection — select exactly one `Smoke.Tests` manifest-bound regular raw report and preserve its SHA-256 through shard copy.
  - Surfaced by: Engineering architecture and security review.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs`, `tools/ForgeTrust.AppSurface.PackageIndex.Tests/PackageArtifactValidationTests.cs`.
  - Verify: missing, malformed, duplicate, unrelated-valid, nonregular, and digest-mismatch cases fail deterministically.
- [x] **T6 (P1, human: ~4h / CC: ~35min)** — Semantic artifact contract — add bounded raw/merged Cobertura fixtures and test every invariant plus merge-only loss.
  - Surfaced by: Engineering test review.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs`, `tools/ForgeTrust.AppSurface.PackageIndex.Tests/PackageArtifactValidationTests.cs`.
  - Verify: all `CPV001` through `CPV011` fixture cases and valid collector path pass/fail as specified.
- [x] **T7 (P2, human: ~2h / CC: ~15min)** — Evidence compatibility — add a versioned scrubbed release artifact while retaining the private detailed Markdown report unchanged.
  - Surfaced by: Engineering security and rollout review.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs`, `tools/ForgeTrust.AppSurface.PackageIndex.Tests/PackageArtifactValidationTests.cs`, package/release documentation.
  - Verify: public artifact contains no absolute paths, credentials, raw XML, commands, or output excerpts; private report keeps existing fields.

## Autoplan Phase 3.5: Developer Experience Review

### DX scope and persona

The product surface is a **developer CLI plus package-maintainer release workflow**. The primary persona is a release maintainer or CI engineer who already owns a .NET repository and needs a trustworthy answer to, “Did the packed CLI collect and preserve real coverage from its disposable consumer?” They tolerate a longer package verification run, but not ambiguity about whether a green result reflects their repository, the packed tool, or a particular coverage driver.

The review uses **DX TRIAGE**. #674 is a bug fix, so the objective is to remove adoption-blocking ambiguity rather than add a playground, hosted service, analytics platform, or new CLI command.

### Developer perspective

I maintain a .NET repository with a coverage gate. I open the CLI README and find the normal path: run `coverage run --dry-run`, run `coverage run`, then run `coverage gate`. That tells me whether my own selected test projects can produce coverage. I also see `collector` as the default and `msbuild` as a compatibility route, but I need the command output to tell me which guarantee I actually received. When a package release is involved, I use `verify-packages`. Today it creates a consumer-proof report, but the report starts with machine directories and a full command ledger. If it says coverage exists, I still have to infer whether it inspected the intended project, whether it saw an exercised branch, and whether merge retained it. I want the first screen of the artifact to say: collector semantic proof passed or failed; raw and merged scopes; selected fixture-relative report; and exactly what to do next. If the run is MSBuild, I need it to say that it proved artifact compatibility only. I should never have to share a report containing my workspace paths or command output to ask for help.

### Evidence-grounded journey and TTHW

| Stage | Developer action | Friction before this plan | Plan resolution |
| --- | --- | --- | --- |
| Discover | Read the CLI coverage quick-start. | Local coverage and packed-release proof are easy to conflate. | A two-level proof recipe names each guarantee and limitation. |
| Install | Restore/install the CLI as documented. | No new install friction. | Unchanged. |
| Local readiness | `coverage run --dry-run` then `coverage run` and `coverage gate`. | Driver assurance must be inferred from options/logs. | The decision tree states collector, MSBuild, merge, and retention guarantees. |
| Packaged proof | Run `verify-packages` for a candidate package. | The detailed report is verbose and unsafe to share. | Stable scrubbed evidence opens with verdict, boundary, scopes, IDs, and next action. |
| Debug | Follow `CPV` or `ASCOV` code. | Package proof failures lack the CLI diagnostics' repair shape. | Every CPV carries cause, next action, and evidence path; docs map it to remediation. |
| Upgrade | Update tool, runner, Coverlet, or ReportGenerator. | Manifest/evidence/schema support is implicit. | Version policy tells the maintainer whether the failure is incompatibility or coverage loss. |

TTHW target: **under two minutes to choose and start the right proof level** from the coverage documentation. This measures decision time, not the package verification process runtime. The magical moment is a one-line scrubbed `passed` result that says an owned class and both branches survived raw-to-merged processing.

### DX implementation contract

The release-facing artifact is exactly `coverage-cli-consumer-proof.evidence.json` with `schemaVersion: 1`. It is produced beside the existing private `coverage-cli-consumer-proof.md`; it does not replace or redact that detailed maintainer report. The schema has only:

```text
verdict, packageVersion, packageArtifactDigest,
driverBoundary { runner, integration, directPackages, assuranceLevel },
raw { outcome, artifactRelativePath, sha256, invariants[] },
merged { outcome, artifactRelativePath, invariants[] },
failures[] { code, scope, cause, nextAction, evidenceRelativePath }
```

The artifact excludes command names/arguments, working directories, NuGet sources/configuration, absolute paths, raw XML, credentials, and arbitrary output excerpts. `CPV` is authoritative for the package consumer semantic proof. `ASCOV` remains authoritative for the underlying public CLI run/merge/gate diagnostic; a CPV `nextAction` may point to the relevant `ASCOV` guide but never replaces it.

The documentation adds one cross-linked “Coverage proof levels and driver boundary” section:

```text
Need to validate your repository's coverage setup?
  -> collector: coverage run --dry-run -> coverage run -> coverage gate
     Guarantee: selected VSTest projects produced normal CLI artifacts.

Need to validate a packed CLI candidate before publication?
  -> PackageIndex verify-packages
     Guarantee: owned Smoke.Tests collector proof retained class/line/branch facts
     from the selected raw report to the merged report.

Need legacy VSTest compatibility?
  -> --coverage-driver msbuild
     Guarantee: artifact compatibility only; not semantic raw-to-merged proof.

Have externally produced matrix shards?
  -> coverage merge -> coverage gate
     Guarantee: fan-in/gate only; not owned collector consumer proof.

Need retention for investigation?
  -> --no-clean only deliberately; retained artifacts may be stale.

MTP selected or runner/package facts conflict?
  -> do not switch drivers to satisfy the proof; capture classification and use
     the separate engine path or issue.
```

The manifest and evidence schemas use additive, versioned evolution. Schema `1` readers reject unknown required structure and unsupported major versions with an actionable incompatibility result. Upgrading Coverlet, ReportGenerator, or runner requires one isolated generated-fixture capture, the package-index fixture suite, and `verify-packages`; an unsupported observed shape is a tool-compatibility result, while a valid shape with missing or zero expected semantics is a coverage defect.

### Eight DX passes

| Pass | Initial | Final target | Evidence and change |
| --- | ---: | ---: | --- |
| 1. Getting started | 5/10 | 9/10 | Add two proof levels, expected artifact, guarantee, and limitation beside the first coverage recipe. |
| 2. CLI design | 8/10 | 9/10 | Keep explicit drivers and existing commands; expose driver boundary/assurance in evidence. |
| 3. Error/debugging | 6/10 | 10/10 | CPV result contains code, scope, cause, next action, and safe evidence location; docs map CPV to ASCOV when appropriate. |
| 4. Documentation | 6/10 | 9/10 | Add one cross-linked proof/driver page section to CLI and package references. |
| 5. Upgrade/migration | 5/10 | 9/10 | Publish manifest/evidence schema policy, supported subset, and upgrade verification procedure. |
| 6. Environment/tooling | 8/10 | 9/10 | Preserve current dry-run, CI, local, and explicit driver workflows; add evidence artifact to existing upload list. |
| 7. Community/ecosystem | 7/10 | 7/10 | Existing public repository, issues, and detailed CLI diagnostics are adequate for this bug-fix scope. No new community surface needed. |
| 8. Measurement/feedback | 5/10 | 8/10 | Capture the two-minute proof-level decision walkthrough and count CPV outcome classes in release validation; no product telemetry is added. |

### DX implementation checklist

- [x] The first CLI coverage example distinguishes local readiness from packaged-consumer semantic proof in three commands or fewer per path.
- [x] `coverage-cli-consumer-proof.evidence.json` schema `1` contains only allowlisted fields and is uploaded with existing package-proof artifacts.
- [x] Every CPV result has a deterministic code, `raw`/`merged` scope, cause, next action, and artifact-relative evidence path.
- [x] The evidence summary presents runner, integration, direct coverage packages, and assurance level before failure detail.
- [x] Docs provide the collector/MSBuild/merge/`--no-clean`/MTP decision tree and name each guarantee.
- [x] Docs specify schema evolution, supported Cobertura subset capture, and package-upgrade verification.
- [ ] Post-implementation verification pending: a maintainer follows the recipe from a clean checkout and records whether choosing the correct proof level took less than two minutes.

### DX independent voice and consensus

The independent `combo/sub` reviewer and primary review agree that the detailed proof renderer cannot be a shared release artifact and that maintainers need a single proof/driver decision path. The Codex CLI voice is unavailable because its local state database cannot be opened read-write.

| Area | Subagent | Primary review | Consensus |
| --- | --- | --- | --- |
| Getting started | Split local readiness from packaged semantic proof. | Same. | Confirmed |
| Error quality | CPV needs cause and next action. | Same. | Confirmed |
| Documentation | Add a proof/driver cross-link instead of broad docs rewrite. | Same. | Confirmed |
| CLI ergonomics | Existing commands are sound; show the selected assurance. | Same. | Confirmed |
| Evidence readability | Versioned scrubbed artifact is release-facing; detailed report stays private. | Same. | Confirmed |
| Upgrade | Schema and fixture-capture policy must be explicit. | Same. | Confirmed |

### DX completion summary

```text
Product type: CLI Tool + package-maintainer workflow
Persona: release maintainer / CI engineer
Mode: DX TRIAGE
TTHW: ambiguous decision path -> under 2 minutes to choose/start the correct proof
Passes: 8 evaluated; critical triage fixes in getting-started and error-evidence paths
Initial overall score: 6/10
Target overall score: 9/10
External voice: subagent ran; Codex unavailable
Unresolved decisions: 0
```

### DX implementation tasks

- [x] **T8 (P1, human: ~1h / CC: ~10min)** — Proof-level documentation — add local-readiness and packaged-semantic recipes with guarantees, limitations, and expected output.
  - Surfaced by: DX getting-started review.
  - Files: `Cli/ForgeTrust.AppSurface.Cli/README.md`, `packages/README.md`, package proof documentation/release notes.
  - Verify: a clean-checkout maintainer chooses and starts the correct proof level in under two minutes.
- [x] **T9 (P1, human: ~2h / CC: ~15min)** — Actionable safe evidence — emit schema-v1 `coverage-cli-consumer-proof.evidence.json` with driver boundary and CPV remediation fields.
  - Surfaced by: DX error quality and evidence readability review.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/CoverageCliConsumerProof.cs`, `tools/ForgeTrust.AppSurface.PackageIndex.Tests/PackageArtifactValidationTests.cs`.
  - Verify: every CPV has code/scope/cause/nextAction/path and forbidden private content is absent.
- [x] **T10 (P2, human: ~1h / CC: ~10min)** — Compatibility guidance — publish driver/escape-hatch decision tree and manifest/evidence/Cobertura upgrade policy.
  - Surfaced by: DX CLI ergonomics and upgrade review.
  - Files: `Cli/ForgeTrust.AppSurface.Cli/README.md`, `packages/README.md`, release notes.
  - Verify: docs state collector/MSBuild/merge/MTP assurances and schema-version behavior.

## Cross-Phase Themes

1. **Identity before interpretation.** CEO, engineering, and DX review all found that a Cobertura file cannot be trusted until it is tied to the intended project, raw report, copied shard, and driver boundary. The manifest-bound selected-artifact model is the shared correction.
2. **Raw and merged evidence are different facts.** The CEO and engineering passes independently require positive fixture semantics before and after fan-in. This prevents collection loss and merge loss from being conflated.
3. **Evidence has two audiences.** Engineering and DX both found the existing detailed renderer useful to maintainers but unsafe and too noisy for a release artifact. Keep it private; publish a versioned allowlist-only evidence document.
4. **Driver semantics must be explicit.** CEO, engineering, and DX agree that collector, MSBuild, merge, and MTP cannot share an implied assurance level. The proof and docs state the boundary rather than silently falling back or overclaiming support.
5. **External reproduction is issue evidence, not release machinery.** The plan hardens the owned package proof whether or not Skoolit evidence is currently available; only the GitHub issue outcome waits for classified external evidence.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR | 15 decisions; selected an owned semantic proof and deferred unrelated platform work. |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | DEGRADED | Codex CLI unavailable because its local state DB is read-only; independent `combo/sub` reviews ran per phase. |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | Manifest-bound identity, provenance hashes, bounded parser tests, and evidence compatibility defined. |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | SKIPPED | No UI scope detected. |
| DX Review | `/plan-devex-review` | Developer experience gaps | 1 | CLEAR | 6/10 → 9/10 target; local/package proof levels, actionable CPV output, and upgrade guidance defined. |

**VERDICT:** CEO + ENG + DX CLEARED — approved for implementation. Codex CLI review remains degraded, with independent `combo/sub` evidence recorded for every executed phase.

NO UNRESOLVED DECISIONS

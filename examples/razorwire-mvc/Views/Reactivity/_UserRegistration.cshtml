<div class="space-y-4">
    <div class="space-y-1">
        <h3 class="text-sm font-bold text-slate-800 uppercase tracking-tight">Identity</h3>
        <p class="text-xs text-slate-500">Pick a name to appear in the live feed.</p>
    </div>
    
    <form id="register-form" asp-action="RegisterUser" method="post" rw-active="true">
        <div class="flex gap-2">
            <input type="text" name="username" id="register-username" class="input-premium" placeholder="Your name..." required autocomplete="off">
            <button class="px-4 py-2 bg-slate-900 text-white text-sm font-semibold rounded-lg hover:bg-slate-800 active:scale-95 transition-all shadow-sm" type="submit">
                Join
            </button>
        </div>
    </form>

    <div class="pt-4 border-t border-slate-100">
        <rw:island id="user-list" src="/Reactivity/UserList" loading="lazy" swr>
            <div class="space-y-4">
                <div class="flex items-center justify-between">
                    <h4 class="text-xs font-bold text-slate-500 uppercase tracking-widest">Active Members</h4>
                    <div class="h-4 w-12 bg-slate-100 rounded-full animate-pulse"></div>
                </div>
                <div class="flex items-center gap-3 text-slate-400 min-h-[80px] items-start pt-2">
                    <div class="animate-pulse h-2 w-2 bg-slate-200 rounded-full mt-1.5"></div>
                    <span class="text-xs italic">Connecting to presence...</span>
                </div>
            </div>
        </rw:island>
    </div>
</div>

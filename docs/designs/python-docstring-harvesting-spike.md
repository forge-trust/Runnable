# Design: Static Python Docstring Harvesting Spike

Generated by /office-hours on 2026-08-21
Branch: main
Repo: forge-trust/AppSurface
Status: APPROVED — bounded C#/Python discovery proof
Mode: Builder

Related issue: [#771](https://github.com/forge-trust/AppSurface/issues/771)

> **Candidate-gate outcome — 2026-08-23:** `TreeSitter.DotNet` `1.3.0` was rejected before source integration because its exact compressed archive is 53,401,399 bytes, exceeding the approved 5 MiB limit. The measured artifact, native inventory, and license/provenance metadata are retained in the [source-controlled candidate gate record](https://github.com/forge-trust/AppSurface/blob/main/Web/ForgeTrust.AppSurface.Docs.Tests/TestData/PythonParserDecision/README.md). Security review superseded the planned local restore/child-process smoke: a local feed is not a sandbox for NuGet build or native code, and the failed static size gate means runtime execution cannot change this rejection. A future runtime check requires a separately approved operating-system-level sandbox design. No parser fallback was selected.

## Problem Statement

Determine whether `ForgeTrust.AppSurface.Docs` can statically document Python that is embedded in or shipped beside a broader .NET codebase. The immediate objective is a credible package-level proof that the product serves more than the direct .NET ecosystem; improving AI answer quality remains a future hypothesis, not this spike's success measure.

## Autoplan Decision Record — 2026-08-21

The CEO challenge found that the original wording could mistake a narrow fixture for general Python support. The user selected **B — bounded cross-language discovery proof**:

- Retain a static C# plus embedded-Python proof, but make the value visible as a thin, explicit C# host-to-Python ownership relationship in generated navigation and search.
- Keep literal `__all__` as the deliberately strict boundary of the positive proof fixture only. It is not a claim that missing `__all__` means a Python module is universally non-public, and this spike does not establish the eventual general Python visibility policy.
- Establish parser package acceptance before source integration. The initial implementation may add the candidate only when its compressed package delta is at most **5 MiB**, it needs no undisclosed native runtime asset, its required runtime identifiers are proven on the supported consumer paths, and its license/provenance review is accepted. These gates deliberately rule out a 50.93 MiB all-grammar native bundle unless a later, explicit product decision revises the budget.
- If the candidate fails a gate, stop before Python source integration, record the measured outcome and consumer evidence, and leave a managed parser as a separately approved follow-up. Do not automatically substitute another parser.
- Do not expand this proof to signatures, type metadata, style parsing, a call graph, runtime inspection, or an AI-answer-quality claim.

This decision record supersedes earlier wording in this document where it implied that `__all__` is the general v1 public-API contract or that a host-to-script relationship is optional.

## What Makes This Cool

A .NET application can show C# and embedded Python as one source-linked documentation surface—navigation, search, source anchors, and diagnostics—without importing, executing, or even requiring a Python runtime. That is a materially different promise from conventional Python documentation generators, which commonly use dynamic import or require a chosen docstring style.

## Constraints

- Parse source files only: do not execute Python, import modules, invoke Python tooling, or depend on a Python runtime.
- In the proof fixture, treat a top-level, literal `__all__` sequence of string names as the intentionally explicit export contract for named module-level classes and functions. Do not infer a general Python visibility rule from this fixture. A module docstring describes the module container; documented members of an exported class are children of that exported API rather than independent module exports.
- Preserve docstring content without interpreting Google, NumPy, Sphinx, or other docstring styles. Remove Python literal delimiters/prefixes, decode the supported literal value, and normalize common indentation so the rendered text is stable without turning it into style-specific structure.
- The negative fixture module with no recognizable `__all__` must emit a nonfatal, actionable public-boundary diagnostic; it must not silently report a successful empty Python API. The diagnostic proves the fixture policy and does not become a blanket product conclusion about every Python module.
- Keep existing path-policy, parser-input-budget, cancellation, source-anchor, health, and diagnostic guarantees intact.
- The proof must use the real Docs aggregation, navigation, and search pipeline beside C#; a parser-only benchmark is insufficient.
- A future parser candidate's actual package contents, licenses/notices, runtime identifiers, package-size delta, and package-consumer behavior must pass the acceptance gate before it becomes a product dependency. Runtime behavior can be acceptance evidence only after a separately approved operating-system-level sandbox design; the current rejected candidate is static-only.
- Do not introduce a generic multi-language harvester abstraction in this spike. The existing JavaScript implementation is the second code-language reference point; generalization can be reconsidered once Python exposes genuine shared seams.

## Premises

- The target environment is a broader .NET codebase with Python embedded or sidecar code; exact production Python conventions are not yet defined.
- The current documentation is largely in code, and Python docstring quality/style is mixed.
- The useful immediate evidence is a small public fixture that shows C# and Python coexisting in the installed package, rather than an internal-source harvest or an AI-answer prototype.
- Python defines a docstring structurally as the first statement of a module, class, function, or method, so raw static extraction is a stable v1 boundary.
- Generated API search qualification is currently JavaScript-specific and must become language-neutral for Python entries to be first-class in search; the proof must also surface the explicit C# host-to-Python ownership relation in both directions.
- Python syntax highlighting is not a spike gate: the current highlighter's safe plaintext fallback avoids a rendering failure while parser and package behavior are proven.

## Cross-Model Perspective

An independent cold read agreed that the fixture can support the claim that AppSurface Docs is an embeddable, polyglot source documentation engine for .NET applications. It identified the `__all__` boundary as the principal sharp edge: a strictly enforced but absent boundary would otherwise create a silent documentation drop. It also identified the generated-API search qualifier, language catalog, configuration/registration, and harvester health/diagnostics as integration seams beyond the parser itself. Its initial preference for a fully managed parser is treated as a risk to test, not a reason to reject Tree-sitter without package evidence.

## Approaches Considered

### Approach A: Tree-sitter vertical product spike — candidate rejected

Only after a future parser passes the static acceptance gate and a runtime sandbox is separately approved, integrate a Python grammar into a minimal harvester and prove the complete AppSurface Docs product path using C# plus embedded Python fixtures. The proof would include static fixture-local `__all__` filtering, raw docstrings, anchors, generated-API search metadata, an explicit host-ownership link, diagnostics, and bounded runtime validation.

### Approach B: ANTLR managed-runtime vertical spike — deferred fallback candidate

Deferred because checking in/generated parser source and owning grammar/code-generation and recovery behavior would front-load more uncertainty than the product proof requires, before native-payload risk has been measured. It remains the explicit follow-up candidate if the Tree-sitter package proof fails.

### Approach C: Parser comparison harness before integration — rejected

Rejected because it can select a library but cannot prove that a package consumer receives useful unified C# and Python documentation, navigation, search, and diagnostics.

## Recommended Approach

The [TreeSitter.DotNet 1.3.0 candidate package](https://www.nuget.org/packages/TreeSitter.DotNet/1.3.0) has been rejected by the stated package acceptance gate: its exact 50.93 MiB archive exceeds the 5 MiB budget. The measured negative result is the complete outcome for this candidate. A managed alternative or another package requires a new decision rather than an automatic fallback.

### Scope

1. **Gate before integration.** Measure the candidate package against the explicit 5 MiB compressed-delta, native-runtime disclosure, and license/provenance requirements, then publish the result. `TreeSitter.DotNet` `1.3.0` failed the static size gate, which is the complete negative result for that candidate. The committed gate therefore performs bounded static inspection only; it never restores, builds, initializes, or executes package content. A future candidate may receive runtime evaluation only after it passes the static gate and a separately approved operating-system-level sandbox design exists.
2. If a future candidate passes that static gate and its runtime sandbox is approved, add a Python source kind, `AppSurfaceDocsPythonHarvestOptions`, path matching, service registration, activation, health participation, and diagnostic integration consistent with the existing code-language harvesters. The options must compose with global path policy and expose `Enabled` (default `true`), empty `IncludeGlobs`/`ExcludeGlobs`, standard default-exclusion controls, `StrictHealth`, and an explicit parser file-size limit. **Python source enumeration requires at least one nonblank, policy-valid `IncludeGlobs` entry:** `Enabled` with no include performs no source reads, publishes no API page, and emits nonfatal `appsurfacedocs.python.missing_include` guidance. This preserves a friction-light, one-setting opt-in while avoiding accidental repository-wide policy warnings before real Python visibility conventions exist. Any approved runtime preflight must occur before source parsing inside a narrow failure boundary, record the active runtime identifier and safe failure detail, and never leave an unhandled native-load exception. The future package review must enumerate the candidate's advertised Windows (`x86`, `x64`, `arm64`), Linux (`x86`, `x64`, `arm`, `arm64`), and macOS (`x64`, `arm64`) native assets. None of this runtime work is authorized for the rejected TreeSitter candidate.
3. Only after those conditions, add a Python harvester that parses files statically with the selected candidate binding and its Python grammar. It must extract module, class, synchronous function, asynchronous function, and method docstrings with source spans. For the spike, a supported docstring is an unprefixed, non-concatenated single- or triple-quoted string; this explicitly excludes `r`/`R`, other prefixed strings, bytes strings, f-strings, and implicit literal concatenation. Preserve source text inside the supported delimiters without Python escape evaluation, then apply a local `inspect.cleandoc`-equivalent normalization: expand tabs at width eight; determine common indentation from subsequent nonblank lines only; remove it; and trim outer blank lines. Comments do not affect the first-statement check; if the first statement in a module, class, function, or method body is not a supported string expression, that entity has no docstring. Traversal tests must cover module and class bodies, module functions, asynchronous functions, class methods, and `decorated_definition` wrappers that contain a class or either function kind; preserve the outer decorated span as the source anchor. Ignore functions and classes nested inside function or method bodies. Convert Tree-sitter's zero-based start row to the Docs source anchor's one-based line number; if a column becomes observable, apply the same one-based conversion. The spike extracts names, hierarchy, source spans, and normalized docstring text only; it does not project structured parameter, annotation, default-value, or return metadata.
4. For the proof fixture only, accept one unannotated top-level literal `__all__ = ["..."]` or tuple equivalent; a type-annotated assignment is invalid in this fixture policy. The boundary publishes matching module-level classes/functions; documented members of an exported class are emitted below their exported parent. An absent boundary emits warning `appsurfacedocs.python.public_boundary_missing` with the repository-relative module path, emits no module API page, and publishes no public symbols. An otherwise empty `__init__.py`—only whitespace, comments, and/or a supported module docstring—is ignored without that warning. Multiple assignments, annotations, `+=`, dynamic expressions, or non-string entries emit warning `appsurfacedocs.python.public_boundary_invalid`, emit no module API page, and publish no public symbols. Names in a valid `__all__` that resolve to a variable, type alias, or import are omitted with `appsurfacedocs.python.export_not_supported`; names with no source declaration emit `appsurfacedocs.python.export_not_found`. Both are warnings. In every boundary failure, the harvester remains nonfatal unless its normal strict-health rules make it blocking.
5. Publish grouped Python API pages beneath `api/python/{module-slug}`. Derive each slug from the policy-approved repository-relative source path, stripping `.py` and a final `__init__` segment, then use the existing URL-safe path-slug rules so segment context survives (for example, `tools/worker.py` becomes `tools-worker` and `tools/__init__.py` becomes `tools`). If two module paths resolve to the same slug, emit warning `appsurfacedocs.python.slug_collision` and publish neither colliding page; never silently merge them. Refactor generated-API search qualification so it validates the existing canonical marker plus language-neutral generated-API provenance and structure, rather than JavaScript-only marker, page-type, and `api/javascript/` predicates. Tests must prove the resulting Python children are marked generated API symbols in search without weakening the existing JavaScript qualification checks.
6. Keep code-fence highlighting on the safe fallback path unless Python grammar registration is genuinely needed to satisfy an existing presentation assertion.
7. Add the fixture to the existing consumer/aggregation test surface:
   - a documented C# host type decorated with the public, source-only marker `[AppSurfacePythonModule("relative/module.py")]`; the Docs output must render a stable reciprocal link between the host's C# symbol anchor and the Python module API page, and each side must be discoverable through generated search;
   - a Python module with a module docstring, literal `__all__`, documented exported class/function, documented method, and excluded internal helper;
   - a separate Python module that omits `__all__` and asserts the boundary diagnostic.
8. Update public/reference documentation and third-party notices for the parser dependency, including a clear statement that harvesting is static and does not execute Python.

### Ownership Boundary

The small relationship proof has one explicit source contract rather than a generic cross-language model:

- `AppSurfacePythonModuleAttribute` is a public .NET marker that may decorate one documented top-level C# type. Its sole argument is one literal, repository-relative `.py` path; it is read from Roslyn syntax and is never instantiated or executed during harvesting.
- Reject a rooted path, a path containing `.` or `..` segments, a non-`.py` path, a nonliteral argument, duplicate markers on one host, or a source path rejected by the shared harvest policy. Each rejection produces a specific nonfatal ownership diagnostic and no link.
- An internal Python-specific ownership linker joins the C# marker's already-proven source anchor with the accepted Python module page after harvesting. It creates reciprocal, HTML-encoded links only when exactly one documented host anchor and one published Python module page match. A missing, unpublished, excluded, or ambiguous target produces a diagnostic and no dangling link.
- Ownership is many-host-to-one-module only if every host independently satisfies the same checks. A Python module never infers a host, no source text is searched heuristically, and no caller/callee/runtime meaning is assigned to the link.
- The linker is deliberately internal and Python-specific. It must not add a public generic relationship field to `DocNode`, `IDocHarvester`, or the search schema; a future language-neutral model needs demonstrated consumers beyond this proof.
- Because harvesters complete independently, the C# marker and Python module source identities must reach the linker through one internal, Python-specific candidate snapshot/provider owned by the aggregation path. It is not a public `IDocHarvester` extension point and must retain only canonical route, source identity, anchor, and validation outcome—not source text or a generic relationship graph.

### Explicit Non-Goals

- Import-time inspection, runtime reflection, Python environment discovery, or executing user code.
- Broad support for dynamically assembled `__all__`, re-exports, wildcard imports, package-level resolution, decorators' runtime effects, or docstring-style semantics.
- A generalized parser framework or a claim of complete Python language/toolchain support.
- Measuring whether harvested docstrings improve AI answers in this spike.

## Open Questions

- `TreeSitter.DotNet` `1.3.0` does not meet the stated package acceptance gate: the recorded 50.93 MiB archive exceeds the 5 MiB budget. Its static rejection is final for this spike; no runtime observation can change it.
- For a future candidate, the supported Python grammar version and the minimal malformed-source recovery cases to turn into contract tests.
- The ownership link is intentionally a literal, test-only association. A durable cross-language reference model is deferred until a real host convention and user demand exist.

## Success Criteria

The remaining product criteria apply only after a future candidate passes static review and receives the separately approved runtime-sandbox decision.

- First, the selected parser candidate either passes the explicit package acceptance gate or produces a published, reproducible negative result; a failure does not advance into source integration.
- Only after a future candidate passes static review and receives runtime-sandbox approval, a clean package consumer may harvest both C# and Python fixtures into one Docs aggregation without a Python runtime installed or Python code executed.
- Exported Python class, function, and method nodes appear with normalized source docstring text, stable source anchors, navigation entries, and generated-API search metadata.
- A symbol absent from literal `__all__` does not appear as public API.
- A module without a recognizable `__all__` emits the agreed nonfatal, actionable diagnostic; no silent successful empty Python harvest occurs.
- A module with a missing or invalid public boundary does not publish an empty Python API page.
- The C# host API page and Python module API page expose reciprocal, source-backed ownership navigation and are both retrievable through generated search; this is a relationship discovery proof, not a runtime call graph.
- Existing path policy, input budget, cancellation, and malformed-source resilience are covered by focused tests.
- A future accepted candidate's NuGet package must contain correctly attributed parser assets, record its size delta, enumerate every advertised native asset, and pass any separately approved sandboxed runtime verification on the defined Windows, macOS, and Linux evidence paths.
- Package size, build, tests, formatting, analyzers, and documentation checks meet the repository's existing release expectations.

## Distribution Plan

This rejected candidate's deliverable is the static, source-controlled evidence record. A future accepted candidate would ship through the existing `ForgeTrust.AppSurface.Docs` NuGet package and its established build and package-validation pipeline; no separate Python package or CLI is planned.

## Next Steps

1. Completed: record the static size, archive, native-asset, and provenance evidence for `TreeSitter.DotNet` `1.3.0`; its size-gate rejection ends this candidate's spike.
2. Do not select or execute a fallback parser automatically. Any new candidate requires a new approved decision and a new static gate record.
3. If a future candidate passes the static gate, separately approve an operating-system-level sandbox before designing or running any restore, build, load, corpus, or package-consumer runtime validation.
4. Only after those two conditions may the deferred Python source/harvester, ownership, search, package, notice, and documentation work resume.

## Reviewer Concerns

The document completed three independent review rounds. The size gate is now resolved by rejection; these remaining concerns apply only to a future candidate after a new decision and sandbox approval:

- A future candidate's native binding may be tested against malformed-source and budget-limit corpora only inside the separately approved operating-system-level sandbox. A process-level native fault there is a rejected candidate result, not a recoverable harvest diagnostic.
- A future candidate's package, bundled grammars, declared native assets, license notices, and runtime behavior on the defined evidence paths must meet release policy. If they do not, record the failure and stop; a managed parser remains a separate, explicitly approved future decision rather than an automatic fallback.

## CEO Review — Selective Expansion

> **Historical planning record:** The material below records the pre-rejection reviews and proposed implementation. It is retained for decision traceability only. The static rejection and runtime-sandbox requirement above supersede every proposed restore, build, child-process, initialization, corpus, or native-load action in that record.

### Decision Audit Trail

| ID | Decision | Basis | Outcome |
|---|---|---|---|
| C1 | Reframe the spike as C#↔Python ownership discovery. | User-selected challenge B. | Included; success requires reciprocal navigation and search. |
| C2 | Keep `__all__` only as a positive/negative fixture contract. | User-selected challenge B. | Included; general Python visibility remains deferred. |
| C3 | Set a 5 MiB compressed package-delta gate before source integration. | Autoplan selective-expansion decision; candidate is 50.93 MiB. | Included; a failed candidate records evidence and stops. |
| C4 | Use a literal C# source attribute instead of inferred relationships or a generic options graph. | Autoplan architecture decision. | Included; it is visible where the host owns the script and has deterministic static semantics. |
| C5 | Do not add signatures, type metadata, style parsing, or an AI-answer eval. | Scope discipline. | Skipped; they do not establish the ownership-discovery claim. |
| C6 | Do not create a managed-parser TODO yet. | No production Python convention or candidate result identifies the right follow-up. | Deferred without a backlog item. |

<!-- AUTONOMOUS DECISION LOG -->

| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|---|---|---|---|---|---|
| A1 | CEO | Require a 5 MiB compressed-delta gate before integration. | Taste / feasibility | Prefer boring | Prevent an all-grammar native bundle becoming unnoticed managed-package debt. | Integrate first and audit later. |
| A2 | CEO | Make the relationship a source attribute and post-harvest link, not a call graph. | Scope | Deliver the slice | Gives an observable user outcome without inventing runtime semantics. | Generic relationship platform. |
| A3 | Eng | Run native initialization and malformed corpus in a child process. | Completeness | Choose completeness | A native signal fault cannot be rescued in the test runner. | In-process exception handling. |
| A4 | Eng | Generalize only the internal generated-API provenance bit with language-qualified canonical checks. | Architecture | Prefer boring | Preserves JavaScript's protection while enabling Python search without a public model change. | Per-language public metadata or a loose generic guard. |
| A5 | Eng | Keep ownership candidates in one Python-specific internal snapshot. | Architecture | Deliver the slice | Coordinates independent harvesters without turning `IDocHarvester` into a graph API. | Reparse C# or add public relationship fields. |
| A6 | DX | Require a nonempty `IncludeGlobs` before Python reads source. | DX / safety | Prefer boring | Prevents accidental repository-wide scanning and diagnostics while retaining one-setting opt-in. | Broad default discovery or `Enabled=false` two-setting activation. |
| A7 | DX | Use generated Docs links as the magical moment and record a five-minute journey. | DX | Deliver the slice | Measures the exact selling point for a .NET maintainer without inventing telemetry. | Parser-only benchmark or product analytics. |

### 1. Architecture Review

**WARNING — accepted design:** two pre-existing harvesters cannot independently express a useful relationship. The plan adds one small, Python-specific post-harvest linker, not a new language framework.

```text
                            package gate (before integration)
NuGet candidate ──measure──> size / native assets / provenance / consumer smoke
                                │ pass                         │ fail
                                ▼                              ▼
 C# source ──Roslyn──> C# host anchor + literal marker      published rejection
                                │                              (stop)
 Python source ──parser──> accepted Python module page
                                │
                                ▼
                 PythonOwnershipLinker (exact, policy-checked join)
                                │
              ┌─────────────────┴─────────────────┐
              ▼                                   ▼
    C# symbol-anchor link                    Python module link
              └─────────────> nav + generated search <─────────────┘
```

The ownership state is finite and stateless across harvest runs:

```text
Discovered marker ──valid/policy-approved──> Awaiting Python page
      │ invalid                                 │ target published exactly once
      ▼                                         ▼
 Diagnostic + no link <── target absent/ambiguous ──> Reciprocal links emitted
```

At 10× files, shared path policy and each parser's input cap protect discovery; the first scaling limit is total parse time, so the existing cancellation and per-file budget remain mandatory. At 100× files, the package should still avoid retaining full source text after a page is produced. There is no auth, database, network endpoint, user mutation, secret, or new data store. The relevant production failure is a native binding failing to load in a supported consumer: it must produce a redacted diagnostic, strict-health result when opted in, and no unhandled exception. Rollback is a package/version revert or disabling Python harvest; the marker is inert when no Python harvester is present.

### 2. Error & Rescue Map

| Codepath | What can go wrong | Rescue action | Reader/operator impact |
|---|---|---|---|
| Candidate package gate | Archive exceeds 5 MiB delta, assets/provenance incomplete, or consumer load fails. | Record measured result; do not add dependency or harvest code. | Clear candidate rejection, no degraded shipped feature. |
| Python native initialization | Binding/ABI/RID load fails after a gate pass. | Catch the documented load failure boundary, redact details, publish `appsurfacedocs.python.native_load_failed`. | Python pages absent; health follows `StrictHealth`; C#/Markdown/JS continue when best-effort. |
| Source enumeration/read | Path is excluded, inaccessible, oversize, cancelled, or malformed. | Reuse shared path/read budgets and existing cancellation behavior; emit established diagnostic family. | No document for that file, never a hung or silently escaped scan. |
| Fixture boundary parse | `__all__` absent, invalid, or contains unsupported export. | Emit the specified nonfatal diagnostic and publish no empty module page. | Actionable fixture-policy warning. |
| Ownership marker parse | Nonliteral, unsafe, duplicate, excluded, missing, unpublished, or ambiguous Python target. | Emit a specific ownership diagnostic and create no link. | No dangling or misleading cross-language link. |
| Search projection | A noncanonical node attempts generated API metadata. | Retain canonical provenance and structure checks, generalized by language. | Ordinary nodes cannot obtain generated-symbol treatment. |

**CRITICAL GAP closed by plan:** process-level native faults are not recoverable .NET exceptions. The package gate must run malformed/budget corpus checks in a separate process and reject the candidate if a native crash occurs.

### 3. Security & Threat Model

| Threat | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Python source executes during documentation harvest. | Low | High | Static parsing only; no interpreter, import, subprocess, or reflection path. |
| Ownership marker escapes repository policy. | Medium | Medium | Literal relative `.py` path only; reject rooted, dot-segment, nonliteral, and policy-excluded values before any read/link. |
| Unsafe source string becomes rendered HTML. | Medium | Medium | Build links only from resolved canonical routes and HTML-encode labels/attributes; never render raw marker text. |
| Native parser supply-chain or ABI vulnerability. | Medium | High | Size/provenance/license/RID gate, consumer smoke, notices, and candidate rejection on native fault. |
| Search metadata privilege bypass. | Low | Medium | Preserve provenance validation; generalize its language predicate rather than make metadata trusted. |

### 4. Data Flow and Interaction Edge Cases

```text
C# attribute / Python file
        │ nil, empty, unsafe, excluded, too large, cancelled, malformed
        ▼
path + syntax validation ──> static parse ──> policy/boundary transform ──> doc nodes ──> linker ──> nav/search
        │                       │                  │                         │              │
        └── diagnostic/no link ─┴── diagnostic ────┴── no empty Python page ──┴── no dangling link
```

There is no new interactive UI flow. Reader-visible edge states are: no Python page when the candidate is rejected; a C# page without a link when the marker is invalid or target is not published; a Python page without a host link when no valid marker resolves; and normal reciprocal links only for the exact match. Tests must assert each state, rather than treating an absent anchor as a successful no-op.

### 5. Code Quality Review

**WARNING — prevent copy/paste architecture:** reuse the JavaScript harvester's options, path-policy, diagnostics, activation, health, and generated-page conventions, but do not copy its lifecycle grammar or introduce `ILanguageDocHarvester`. The linker may depend on existing C# source provenance and Python module source identity only. `DocNode`, `IDocHarvester`, and the public search schema keep their shapes; this reduces compatibility risk and prevents a one-language proof from locking a premature abstraction.

### 6. Test Review

```text
New codepaths
  package gate: size, assets, notices, load, malformed/budget isolated corpus
  Python harvest: docstrings, classes/functions/methods, source anchors, boundary outcomes
  ownership: valid marker, each invalid marker, target absence, multiple hosts, reciprocal links
  generated search: canonical C#, JavaScript regression, canonical Python, forged metadata rejection
  consumer: packaged C# + Python fixture without Python installed/executed
```

The 2 a.m. confidence test is an installed consumer package on each declared evidence path that either emits both linked pages or reports one precise diagnostic with no native crash. Hostile QA should use a path traversal marker, an excluded target, malformed Python around valid declarations, a `__all__` dynamic expression, duplicate host attributes, and a collision-inducing module path. The chaos test runs the malformed/budget corpus in a child process and treats abnormal exit as candidate rejection. No external service, clock, random input, or result ordering belongs in these tests.

### 7. Performance Review

No database, query, cache, or background-job change is proposed. The package-gate archive inspection is bounded and runs in release verification, not per request. Harvest uses the existing candidate enumeration and maximum file size; ownership joining is linear in published Python module identities plus marker count. The package size delta is the dominant distribution performance concern, hence the hard gate. Add a large policy-approved Python fixture/budget test; do not claim parser throughput until measured in the consumer package.

### 8. Observability and Debuggability Review

Each rejected candidate produces a retained artifact recording archive hash/version, compressed delta, asset inventory, licenses/notices, host RID, consumer result, and abnormal-process status. Runtime harvest diagnostics use existing redaction rules: record diagnostic code, language/source-kind aggregate, and safe guidance—never source content, raw exception, or unchecked path. The existing harvest health/observatory must identify Python as a built-in participant only after its source kind is registered. No alert/dashboard is warranted for a fixture proof.

### 9. Deployment and Rollout Review

There is no schema migration. The rollout sequence is: (1) candidate gate, (2) dependency/implementation only on pass, (3) package and consumer validation, (4) documentation/notices, (5) release under existing package validation. A failed gate is a stop, not a degraded release. Roll back by reverting the isolated package change or setting Python harvesting disabled; no repository source needs mutation. Post-package smoke: run the consumer with no Python runtime, confirm linked C#/Python pages/search, and confirm the negative module's diagnostic.

### 10. Long-Term Trajectory Review

Reversibility is **5/5** before dependency integration and **4/5** after it. The remaining deliberate debt is a fixture-only visibility policy and source marker; both are documented and bounded rather than hidden defaults. In twelve months, a new engineer should see a measured candidate result, a source-only ownership contract, and an explicit decision that broader Python semantics require real adoption evidence. Phase 2, if demanded, is a separately approved managed-parser or visibility-policy design—not an extension accidentally implied by this spike.

### 11. Design and UX Review

Skipped: the plan adds no product UI, layout, interaction, or visual design. Existing generated navigation and search are the proof surface; link text must be descriptive and keyboard-accessible under the current renderer, but a visual design review is not indicated.

### What Already Exists

- `CSharpDocHarvester` already publishes source-anchored symbol provenance from Roslyn syntax.
- `JavaScriptDocHarvester` already demonstrates enabled options, path policy, diagnostics, strict-health participation, grouped generated pages, and generated symbol records.
- `DocAggregator` already owns aggregation, navigation, search projection, health, diagnostics, and the canonical generated-symbol guard that must become language-neutral.
- The Docs consumer fixture and package-index tooling already provide package-consumer and artifact-validation seams.

### Not in Scope

- General Python publicness without `__all__`: requires a real codebase convention.
- Signatures, annotations, default values, structured style parsing, and type references: do not affect the ownership proof.
- Arbitrary cross-language graph relationships, call graphs, import resolution, or runtime semantics: unsupported by the static contract.
- Automatic managed-parser fallback: a different dependency strategy requires a fresh product decision.
- AI answer-quality evaluation: the future hypothesis is neither measured nor promised here.

### Dream State Delta

The long-term product could let teams reliably discover deliberately related source across languages. This plan proves a single static ownership edge, source anchors, navigation, and search. It leaves automatic discovery, broad Python support, relationship semantics, and answer-quality measurement intentionally unclaimed.

### Failure Modes Registry

| Codepath | Failure mode | Rescued? | Test? | User sees? | Logged? |
|---|---|---:|---:|---|---:|
| Candidate gate | Size/provenance/native/RID failure | Yes—stop candidate | Yes | Published rejection | Yes |
| Native parser process | Unhandled native crash | Yes—child-process rejection | Yes | Candidate rejected | Yes |
| Python source read | Path/budget/cancel/malformed failure | Yes | Yes | Existing diagnostic/health semantics | Yes |
| Fixture `__all__` | Missing or invalid boundary | Yes | Yes | Actionable diagnostic, no empty page | Yes |
| Ownership marker | Unsafe or unresolved relationship | Yes | Yes | Actionable diagnostic, no link | Yes |
| Generated search | Forged/noncanonical metadata | Yes—omit metadata | Yes | Normal search record only | No |

No row is a **CRITICAL GAP**: every failure either prevents dependency integration, emits an actionable diagnostic without a dangling link, or uses the existing bounded harvest behavior.

### CEO Implementation Tasks

- [ ] **T1 (P1, human: ~2h / CC: ~15min)** — package gate — measure TreeSitter.DotNet 1.3.0 package delta, native assets, licenses/provenance, and isolated consumer load/corpus behavior before editing Docs source.
  - Surfaced by: Sections 1–3, 7–9 — native 50.93 MB candidate is an operational decision, not an assumed dependency.
  - Files: package verification tooling, `Web/ForgeTrust.AppSurface.Docs.ConsumerFixture`, `Web/ForgeTrust.AppSurface.Docs/THIRD-PARTY-NOTICES.md` only if the candidate passes.
  - Verify: archive manifest and size report plus supported consumer evidence; fail stops the spike.
- [ ] **T2 (P1, human: ~3h / CC: ~30min)** — static Python harvest — after T1 passes, add the Python source kind/options/harvester with raw-docstring, fixture-local boundary, budget, health, and diagnostic tests.
  - Surfaced by: Sections 2, 4, 5, and 6 — extraction must remain static, bounded, and never silently publish an empty module page.
  - Files: `Web/ForgeTrust.AppSurface.Docs`, `Web/ForgeTrust.AppSurface.Docs.Tests`.
  - Verify: focused harvester/options tests and solution coverage.
- [ ] **T3 (P1, human: ~2h / CC: ~20min)** — explicit ownership proof — add the source-only C# marker and internal linker, then generalize generated-search provenance without expanding public models.
  - Surfaced by: Sections 1, 3–6 — two independent source trees do not prove cross-language discovery.
  - Files: `CSharpDocHarvester`, `DocAggregator`, Python harvester/linker, `DocModels` only if an existing internal seam cannot carry the candidate, and focused tests.
  - Verify: valid/invalid marker matrix, reciprocal links, no dangling links, and JavaScript search regressions.
- [ ] **T4 (P1, human: ~90min / CC: ~15min)** — package-consumer proof — add the C# host/Python positive and negative fixture and exercise navigation/search/diagnostics from the packaged consumer.
  - Surfaced by: Sections 6 and 9 — the user outcome is packaged product discovery, not parser output.
  - Files: `Web/ForgeTrust.AppSurface.Docs.ConsumerFixture`, Docs consumer/package tests.
  - Verify: consumer runs without a Python runtime and proves the link/search/diagnostic contract.
- [ ] **T5 (P2, human: ~45min / CC: ~10min)** — public contract documentation — document static limits, ownership marker, diagnostics, configuration, and third-party obligations.
  - Surfaced by: Sections 3, 8, and 10 — the safety and scope boundaries are public behavior.
  - Files: `Web/ForgeTrust.AppSurface.Docs/use-appsurface-docs.md`, package notices, repository/package reference docs.
  - Verify: documentation links/build checks and package notice validation.

### CEO Completion Summary

| Area | Result |
|---|---|
| Mode | Selective expansion: accepted one ownership link and a package gate; rejected broad Python product scope. |
| Architecture | One internal Python-specific linker; no generic abstraction. |
| Error/rescue | Six codepaths mapped; native process fault is a candidate rejection, not an in-process rescue. |
| Security | Static-only trust boundary and strict literal path validation required. |
| Tests | Package gate, source, ownership, search, and consumer matrices required. |
| Performance | Package size is the primary risk; per-file parse limits remain inherited. |
| Observability | Retained gate evidence plus redacted normal diagnostics. |
| Deployment | Gate before integration; isolated rollback, no migration. |
| Design | Skipped—no product UI scope. |
| Unresolved decisions | None. |

### CEO Consensus Table

| Topic | Codex outside review | Independent subagent | Resolution |
|---|---|---|---|
| Native package risk | 50.93 MB native bundle may invalidate the wedge. | Managed-parser preference; native package is material debt. | Strong agreement: hard gate before integration. |
| `__all__` semantics | Mandatory general boundary would hide normal Python APIs. | Same concern. | User-selected bounded fixture-only rule; no general policy claim. |
| Product usefulness | Two harvested trees are not a cross-language product story. | Raw docs alone lack a visible relationship. | User-selected explicit C#→Python ownership discovery proof. |
| Signatures/metadata | Suggested broader utility work. | Suggested basic signature extraction. | Deferred: it changes the proof from discovery into a Python documentation product. |

**Consensus:** 3 high-confidence shared concerns accepted; 1 broader-feature recommendation deliberately deferred by the bounded proof decision.

## Engineering Review — Full Architecture and Test Pass

**Prior learning applied:** `evidencehost-risk-obligation-closure` (confidence 10/10) — a producer's success is only a proxy. The parser gate therefore requires direct evidence of package size, native asset identity, child-process loading, and malformed/budget behavior; a green restore alone cannot establish the package claim.

### Architecture findings and resolutions

| ID | Finding | Evidence | Resolution |
|---|---|---|---|
| E1 | **P1 (confidence 10/10):** current packaged Docs proof cannot prove native loading. | `DocsPackageConsumerProof.cs:40-42` says it “does not build an application”; its workflow restores and inspects the graph. | Add a purpose-built isolated package smoke executable to the package-index proof. It must restore the packed artifact, build, initialize the binding, parse the fixed corpus, and report exit code, RID, stderr, asset inventory, and package delta. Abnormal exit rejects the candidate. |
| E2 | **P1 (confidence 10/10):** the aggregation seams are hardcoded for three built-ins and JavaScript-only strict health. | `DocAggregator.cs:1532-1538`, `1548-1570`, and `1573-1588` enumerate built-in progress, JavaScript blocking diagnostics, and typed context dispatch. | Add Python to all three seams and give Python a dedicated strict-blocking diagnostic set. Include Python in source-kind, path-policy, options normalization/validation, and DI registration matrices. |
| E3 | **P1 (confidence 10/10):** generated search qualification is deliberately JavaScript-specific. | `DocAggregator.cs:2475-2482` requires `HasJavaScriptApiLifecycleProvenance`, `api/javascript/`, and `javascript-` page type. | Rename the internal flag to `HasGeneratedApiLifecycleProvenance`; keep it inaccessible to custom harvesters. Use explicit canonical branches for `javascript` and `python`, each requiring matching internal provenance, `api/{language}/` route, `{language}-` page type, fragment parent shape, and canonical marker. Preserve all JavaScript regression cases. |
| E4 | **P1 (confidence 9/10):** neither independent harvester can construct reciprocal links while it runs. | `CSharpDocHarvester.cs:229-236` emits fragment `DocNode`s; `DocAggregator.cs:1274-1303` runs harvesters independently before aggregation. | Use the internal Python-specific ownership candidate snapshot described above and link only after all results are collected. Do not add public relationship members to `DocNode` or a generic harvester interface. |
| E5 | **P1 (confidence 10/10):** Python must be routed through source-specific policy and configuration, not discovered by an ad hoc file scan. | `AppSurfaceDocsHarvestPathPolicy.cs:39-71`, `520-531`, and `586-594` hold three explicit policies/switch cases; `AppSurfaceDocsServiceCollectionExtensions.cs:284-289` and `484-517` register the built-ins. | Add `Python` to the source kind, policy field/constructor/base candidate/switches, `Harvest.Python` option, normalization/validation, service registration marker, and test matrices. |
| E6 | **P2 (confidence 9/10):** code-language catalog registration is not a harvest requirement. | `AppSurfaceDocsCodeLanguageCatalog.cs:9-37` is a Markdown code-fence/TextMate catalog; the plan does not require Python syntax highlighting. | Keep Python fence rendering on the existing plaintext-safe fallback. Use metadata token `python`, whose label has a safe fallback, and add a catalog/grammar entry only if a presentation test demonstrates it is required. |

### Engineering architecture

```text
Phase 0 — candidate gate (new isolated package smoke process)
  packed nupkg ──> archive manifest + compressed delta + notices/provenance
       │
       └──> restored/buildable temporary consumer ──> binding init + fixed corpus
                    │ normal exit + all limits             │ non-zero/signal/timeout
                    ▼                                      ▼
              source integration may start           publish candidate rejection; STOP

Phase 1 — source harvesting (only after pass)
  AppSurfaceDocsOptions.Harvest.Python
       │
  shared PathPolicy(Python) ──> PythonDocHarvester ──> Python DocNodes + internal module candidates
       │                                  │
  CSharpDocHarvester ──> C# DocNodes + internal ownership-marker candidates
                                      │
                                      ▼
             DocAggregator post-harvest PythonOwnershipLinker
                                      │
                    exact, policy-approved matches only
                                      │
                                      ▼
                linked C# fragments + linked Python pages ──> nav/search
```

The public marker is `ForgeTrust.AppSurface.Docs.AppSurfacePythonModuleAttribute`, with `AttributeUsage` restricted to types and one `string` argument. The harvester recognizes only the exact package marker name in source syntax; it never instantiates the attribute. It accepts a decorated type only when that type has a published C# fragment anchor. The static linker uses the fragment `Path`/`ParentPath` pattern already emitted by `CSharpDocHarvester`; it does not attach links to a namespace page by text search.

### Execution and coverage map

```text
PACKAGE GATE (child process)                          HARVEST + OWNERSHIP
[→E2E] packed nupkg                                   [+] PythonDocHarvester
  ├─ size <= 5 MiB delta                                ├─ [UNIT] source kind/options/path-policy
  ├─ asset/notice/provenance inventory                  ├─ [UNIT] module/class/function/async/method docs
  ├─ [→E2E] restore + build smoke host                  ├─ [UNIT] supported/unsupported literal forms
  ├─ [→E2E] binding init + malformed/budget corpus      ├─ [UNIT] valid/missing/invalid `__all__`
  └─ [UNIT] abnormal exit/timeout => reject             ├─ [UNIT] source anchors/slug collision/empty init
                                                        ├─ [UNIT] activation/progress/strict health
                                                        └─ [UNIT] ownership marker validation matrix

SEARCH + PRODUCT PROOF
  ├─ [UNIT] JS canonical generated symbol stays qualified
  ├─ [UNIT] forged/mismatched JS/Python metadata rejected
  ├─ [UNIT] canonical Python fragment gets generated metadata
  ├─ [UNIT] one valid host/module gives reciprocal encoded links
  ├─ [UNIT] missing/excluded/ambiguous/duplicate marker gives diagnostic + no link
  └─ [→E2E] packaged consumer exposes C# + Python search/navigation/negative diagnostic

COVERAGE TARGET: every listed branch must have a deterministic xUnit test; package smoke
uses a process boundary rather than mocking native initialization. No LLM evaluation, clock,
randomness, external service, or browser interaction is in scope.
```

### Test-plan detail

| Area | Required assertions |
|---|---|
| Gate | Exactly measured compressed package delta; every native asset is inventoried and attributed; child process reports its RID; normal, non-zero, timeout, and malformed/budget outcomes each have deterministic reports. |
| Options/policy | Defaults, `Enabled`, include/exclude/default exclusions, invalid values, `.py` inclusion, VCS-ignore interaction, file size, and strict health. |
| Python parse | First-statement semantics with comments, module/class/sync/async/method/decorated definitions, nested-definition exclusion, one-based anchors, all supported/unsupported literal forms, malformed recovery, cancellation, and slug collision. |
| Boundary | Valid fixture `__all__`, missing/invalid/multiple/annotated/dynamic boundaries, empty `__init__.py`, unresolved/unsupported exports, and no empty page. |
| Ownership | Exact marker name, source-only/no instantiation, literal safe path, rooted/dot-segment/non-`.py`/nonliteral/duplicate paths, undocumented host, excluded/missing/unpublished/ambiguous target, many hosts, reciprocal HTML-encoded links, and no dangling link. |
| Integration | Progress, activation, strict health, diagnostics, Docs model clone preservation, JavaScript search regressions, Python generated search, navigation, package consumer without Python installed/executed, notices and release artifact validation. |

### Failure modes after engineering review

| Codepath | Realistic production failure | Test | Error handling | Silent? |
|---|---|---:|---|---:|
| Gate smoke process | Native ABI fault terminates child process. | Yes | Parent records abnormal exit and rejects candidate. | No |
| Gate package proof | Restore succeeds but native assets fail at initialization. | Yes | Build/run smoke requirement catches it; restore-only path is not accepted. | No |
| Python registration | Python omitted from a hardcoded enum/switch/DI/progress path. | Yes | Matrix tests assert registration, policy, health, and progress. | No |
| Strict health | Python warning is unintentionally ignored. | Yes | Explicit Python blocking-code branch. | No |
| Search guard | Loose refactor gives forged metadata an API marker. | Yes | Exact language/provenance/route/page/fragment checks. | No |
| Ownership link | Stale, excluded, or malicious path becomes a dangling/unsafe link. | Yes | Literal normalization, policy resolution, exact join, diagnostic/no link. | No |

No **CRITICAL GAP** remains: each nontrivial failure has a concrete test and an observable rejection or diagnostic.

### Parallelization strategy

| Step | Modules touched | Depends on |
|---|---|---|
| Candidate gate | `tools/ForgeTrust.AppSurface.PackageIndex/`, package metadata/notices | — |
| Python source integration | `Web/ForgeTrust.AppSurface.Docs/`, `Web/ForgeTrust.AppSurface.Docs.Tests/` | Gate pass |
| Ownership/search integration | Docs models/services/tests | Python source identities and gate pass |
| Consumer product proof | consumer fixture, package-index tests | Python and ownership implementation |
| Documentation | Docs docs/notices | final dependency decision |

**Lane A:** candidate gate (independent, first). **Lane B:** source integration → ownership/search (sequential; both touch Docs services/models). **Lane C:** consumer product proof (after B; it requires the new runtime behavior). **Lane D:** documentation/notices (after dependency decision; can overlap late B/C only when the candidate passed). This is mostly sequential by design; parallel worktrees would create conflicts in `Web/ForgeTrust.AppSurface.Docs/` and premature documentation for a rejected candidate.

### Engineering Implementation Tasks

- [ ] **T6 (P1, human: ~3h / CC: ~30min)** — executable package gate — extend package validation with an isolated, packed-package smoke host and a machine-readable candidate report; do not treat restore-only proof as runtime evidence.
  - Surfaced by: E1 — current proof explicitly does not build an app.
  - Files: `tools/ForgeTrust.AppSurface.PackageIndex/`, its tests, package proof docs.
  - Verify: candidate report covers size/assets/notices/RID/normal and abnormal child-process outcomes.
- [ ] **T7 (P1, human: ~4h / CC: ~45min)** — Python source integration matrix — add `Python` options/source kind/path policy/registration/progress/health/diagnostics as one cohesive vertical slice after T6 passes.
  - Surfaced by: E2 and E5 — the existing pipeline has explicit three-language hardcoded seams.
  - Files: Docs options/services/models/tests.
  - Verify: xUnit matrix proves every `Python` branch and strict-health result.
- [ ] **T8 (P1, human: ~3h / CC: ~30min)** — canonical language-qualified generated search — replace the JavaScript-named internal provenance bit with a protected language-qualified guard and retain all JavaScript regressions.
  - Surfaced by: E3 — Python needs first-class generated search without a ranking bypass.
  - Files: `DocModels.cs`, `DocAggregator.cs`, JavaScript/Python aggregation tests.
  - Verify: canonical JS/Python qualify; every forged or mismatched combination is omitted.
- [ ] **T9 (P1, human: ~3h / CC: ~30min)** — source-only ownership linker — implement the typed marker, internal candidate snapshot, and post-harvest reciprocal-link resolution without a public graph model.
  - Surfaced by: E4 — harvest order prevents either harvester linking independently.
  - Files: C# harvester, Python harvester/linker, aggregator, Docs tests.
  - Verify: full ownership error matrix, encoded reciprocal routes, and no dangling links.
- [ ] **T10 (P1, human: ~2h / CC: ~20min)** — packaged product fixture — run the real C#/Python positive/negative proof from a package consumer, separate from the restore proof.
  - Surfaced by: E1 and execution map — claim requires runtime package evidence.
  - Files: consumer fixture/proof host, package-index tests, Docs consumer tests.
  - Verify: no Python runtime/code execution; C#↔Python navigation/search plus negative diagnostic.

### Engineering Completion Summary

| Area | Result |
|---|---|
| Architecture | Six findings folded into the plan; package smoke and post-harvest linker are load-bearing. |
| Code quality | No generic harvester or relationship model; one internal candidate snapshot only. |
| Tests | Full deterministic xUnit map plus isolated package E2E process proof. |
| Performance | Gate and package delta before source work; inherited per-file budgets and linear join. |
| Critical gaps | 0 after adding child-process proof and explicit Python integration matrices. |
| Unresolved decisions | None. |

### Engineering Consensus Table

| Topic | In-host engineering review | Independent subagent | Resolution |
|---|---|---|---|
| Native validation | Restore cannot prove native load; isolate process. | Same; abnormal process exit rejects candidate. | Accepted as the package gate's executable proof. |
| Aggregation | Python must be added to hardcoded progress/health/dispatch seams. | Same. | Accepted as one vertical integration matrix. |
| Search | Preserve canonical checks while adding Python. | Same; recommends explicit language branches. | Accepted with an internal generalized provenance bit plus strict language-qualified checks. |
| Ownership timing | Requires post-harvest coordination without public graph models. | Same. | Accepted as Python-specific candidate snapshot/linker. |
| Syntax highlighting | Safe fallback is sufficient. | Suggested catalog update. | Deferred: the catalog is a Markdown/TextMate concern and no presentation assertion requires it. |

**Consensus:** 4/5 engineering concerns confirmed; 1 presentation expansion deferred as nonessential.

## Developer Experience Review — DX Polish

### Developer Persona Card

**Persona:** a .NET application maintainer who owns a broader C# application with a small embedded or sidecar Python surface. They already use AppSurface Docs or can add it to an ASP.NET Core host; they do not want to adopt a Python runtime, a second documentation site, or a generic graph product just to explain the relationship between their host wrapper and its script.

**Job:** “Show me where this C# host owns Python, let me traverse the two source-backed API pages, and tell me clearly when the package or source contract cannot prove that relationship.”

### Developer Empathy Narrative

“I maintain a .NET service with a Python worker tucked beside it. I can read either source tree, but it is too easy to lose the handoff between the C# wrapper and the script. I want one explicit place to declare ownership, then I want my existing Docs site to show it. I will abandon this if I must install Python, guess whether every incidental `.py` gets scanned, or decipher a native-loader stack trace. I will trust it if the quickstart shows one safe include boundary, one marker, one visible link, and an error that names the next action.”

### Competitive DX Benchmark

| Experience | Current pre-plan | Plan target | Rationale |
|---|---:|---:|---|
| Static safety | 6/10 | 9/10 | Explicit no-execution contract and package gate reduce the usual Python-doc-tool uncertainty. |
| Setup/first useful result | 2/10 | 8/10 | One configuration boundary plus one source marker and an existing Docs host yields the first linked page. |
| Cross-language discoverability | 1/10 | 8/10 | Reciprocal navigation/search provides the exact proof rather than two independent trees. |
| Failure guidance | 4/10 | 9/10 | Missing include, boundary, ownership, and candidate-gate outcomes are named and documented. |
| Ecosystem breadth | 4/10 | 5/10 | This remains a narrow .NET-hosted Python proof; no general Python toolchain claim. |

**Competitive tier:** competitive source-preview proof for an existing AppSurface Docs host. This is intentionally not a replacement for Sphinx, pdoc, or a Python IDE.

### Magical Moment Specification

The delivery vehicle is a generated Docs link, not a demo video or parser output. In a running Docs host, the maintainer opens a documented C# wrapper, sees **Owned Python module: `scripts/worker.py`**, follows it to `api/python/scripts-worker`, and follows the reciprocal **Owned by C# host** link back. Search finds either side. The two source anchors make the relationship inspectable rather than magical-by-assertion.

### Three-Step Golden Path

After the package candidate has passed and a released Docs package contains the feature:

1. **Bound Python scanning.** Add one opt-in boundary to the existing host configuration:

   ```json
   {
     "AppSurfaceDocs": {
       "Harvest": {
         "Python": { "IncludeGlobs": [ "scripts/**/*.py" ] }
       }
     }
   }
   ```

   Expected result: the host starts without Python execution. If the glob is absent, the harvest view reports `appsurfacedocs.python.missing_include` with the exact configuration key and example.

2. **Declare ownership in source.** Put `[AppSurfacePythonModule("scripts/worker.py")]` on one documented C# host type and make the fixture module's intended exports literal in `__all__`.

3. **See the relationship.** Run the existing Docs host, search for the C# host or Python symbol, then follow the reciprocal generated links and source anchors. Expected result: one C#↔Python relationship, no Python import/process/runtime execution, and a diagnosable no-link state when policy/boundary validation fails.

Target time to this result is **three steps and five minutes or less** for a maintainer who already has an AppSurface Docs host. The first released guide must state that this time excludes a rejected parser candidate and initial app creation.

### DX Passes

| Pass | Before | After | Resolution |
|---|---:|---:|---|
| 1. Getting started | 2/10 | 8/10 | Ship the three-step golden path with expected links and a no-include recovery message. |
| 2. API design | 3/10 | 8/10 | One exact `AppSurfacePythonModuleAttribute` and one required `IncludeGlobs` boundary; no inference or configuration matrix. |
| 3. Errors/debugging | 5/10 | 9/10 | Every Python diagnostic supplies problem, policy/source location where safe, remediation, and reference link; package gate publishes child-process evidence. |
| 4. Documentation/learning | 3/10 | 9/10 | Add reference plus copy-paste guide, static limitations, fixture-specific `__all__`, ownership examples, and generated-link screenshot/text fixture. |
| 5. Upgrade path | 7/10 | 9/10 | Additive release: empty `IncludeGlobs` performs no read, so existing hosts do not begin scanning Python until they opt in. Document no automatic fallback on candidate rejection. |
| 6. Dev environment/tooling | 5/10 | 8/10 | No Python prerequisite; deterministic xUnit and packed child-process verification across supported evidence paths. |
| 7. Community/ecosystem | 7/10 | 7/10 | Existing open-source issue/discussion and contribution routes remain sufficient for a proof; no community expansion is justified. |
| 8. Measurement/feedback | 2/10 | 8/10 | Add a repeatable timed golden-path check and record package-gate/diagnostic evidence; defer product telemetry. |

### Error-message contract

| Code | What the maintainer sees | Required fix guidance |
|---|---|---|
| `appsurfacedocs.python.missing_include` | Python is enabled but no source was read. | Set `AppSurfaceDocs:Harvest:Python:IncludeGlobs` to a repository-relative `.py` boundary. |
| `appsurfacedocs.python.native_load_failed` | The accepted dependency could not initialize on this runtime. | Show active RID, safe failure summary, and package-gate/compatibility reference without raw stack trace. |
| `appsurfacedocs.python.public_boundary_missing` / `public_boundary_invalid` | The proof fixture has no supported literal export boundary. | Explain the fixture-only `__all__` syntax and that it does not define general Python publicness. |
| Ownership diagnostic | The host marker was unsafe, invalid, unresolved, excluded, unpublished, or ambiguous. | Name the safe relative path/marker rule and whether to change the marker, include boundary, or source export. |
| Candidate rejection report | The parser is not accepted for the package. | Show measured size/assets/RID/process reason and state that no Python harvesting was installed. |

### Developer Journey Map

```text
Minute 0       Minute 1                  Minutes 2–3                         Minutes 4–5
read guide ──> add IncludeGlobs ──> add source marker + literal fixture ──> open/search/follow link
    │                │                             │                                    │
    │                └─ missing include: exact key │                                    └─ target unavailable: diagnostic/no link
    │                                              └─ invalid boundary: fixture guidance
    └─ rejected candidate: evidence report; no false promise and no fallback
```

### First-Time Developer Confusion Report

| Likely confusion | Resolution in the plan |
|---|---|
| “Does this run my Python?” | State source-only behavior in the first paragraph and assert it in tests. |
| “Why did nothing happen after upgrade?” | `missing_include` tells them the exact opt-in key and gives a glob example. |
| “Why is a valid-looking Python module absent?” | Explain the fixture-local literal `__all__` policy and point to the boundary diagnostic. |
| “Is this a call graph or Sphinx replacement?” | Label links as explicit ownership only; document non-goals beside the example. |
| “What does a native failure mean?” | Parent-process evidence report distinguishes candidate rejection from normal source diagnostics. |

### DX Not in Scope

- Python project/environment discovery, a Python CLI, or automatic `pyproject.toml` detection: conflicts with static package-only scope.
- Auto-inferred C#↔Python calls/imports: too ambiguous for the single explicit ownership claim.
- A visual playground, tutorial app, or new dashboard: existing Docs host and consumer fixture are the appropriate proof surfaces.
- Telemetry/NPS or broad community programs: record a repeatable TTHW check first; no adoption base yet justifies instrumentation.
- Python syntax highlighting: plaintext-safe fallback is sufficient until a presentation need is demonstrated.

### DX Implementation Tasks

- [ ] **T11 (P1, human: ~90min / CC: ~15min)** — golden-path documentation — publish the three-step configuration/marker/link guide, with expected output and static/no-runtime statement.
  - Surfaced by: Passes 1, 2, and 4 — the capability is not adoptable without one copy-paste path.
  - Files: `Web/ForgeTrust.AppSurface.Docs/use-appsurface-docs.md`, package README/reference docs, consumer fixture docs.
  - Verify: a maintainer follows the guide against the packaged fixture in <=5 minutes.
- [ ] **T12 (P1, human: ~60min / CC: ~10min)** — error and upgrade reference — document every Python/ownership/gate diagnostic, the explicit include opt-in, additive upgrade behavior, and no-fallback result.
  - Surfaced by: Passes 3 and 5 — avoid generic native failures and surprise scanning.
  - Files: Docs reference/diagnostic documentation and release guidance.
  - Verify: error strings link to one reference section and each has problem, cause, and safe fix.
- [ ] **T13 (P2, human: ~45min / CC: ~10min)** — measurable developer journey — add a deterministic timed acceptance checklist to the package consumer proof/report.
  - Surfaced by: Pass 8 — preserve evidence for future AI-answer-quality and adoption hypotheses without adding telemetry.
  - Files: package-index proof docs/tests and the consumer fixture guide.
  - Verify: record an observed TTHW and whether the C#↔Python link/search/diagnostic contract passed.

### DX Scorecard

```text
+====================================================================+
|              DX PLAN REVIEW — SCORECARD                             |
+====================================================================+
| Dimension            | Score  | Prior  | Trend  |
|----------------------|--------|--------|--------|
| Getting Started      | 8/10   | —      | ↑      |
| API/CLI/SDK          | 8/10   | —      | ↑      |
| Error Messages       | 9/10   | —      | ↑      |
| Documentation        | 9/10   | —      | ↑      |
| Upgrade Path         | 9/10   | —      | ↑      |
| Dev Environment      | 8/10   | —      | ↑      |
| Community            | 7/10   | —      | →      |
| DX Measurement       | 8/10   | —      | ↑      |
+--------------------------------------------------------------------+
| TTHW                 | unmeasured → <=5 minutes for an existing host |
| Competitive Rank     | Competitive source-preview proof              |
| Magical Moment       | designed via reciprocal generated Docs links  |
| Product Type         | .NET library + generated documentation         |
| Mode                 | DX POLISH                                     |
| Overall DX           | 8.3/10                                        |
+====================================================================+
```

The score is conditional on the gate passing and the documentation/test tasks landing. If the candidate is rejected, the correct DX outcome is a clear evidence report—not a pretend feature score.

### DX Implementation Checklist

```text
DX IMPLEMENTATION CHECKLIST
============================
[ ] Time to hello world <=5 minutes for an existing Docs host
[x] Installation remains the existing Docs package; no Python runtime install
[ ] First run produces reciprocal C#↔Python navigation/search output
[x] Magical moment is defined via generated Docs links
[ ] Every Python/ownership/gate diagnostic has problem + cause + fix + reference link
[x] `IncludeGlobs` and `AppSurfacePythonModuleAttribute` names are guessable from one example
[x] Empty include default is safe; explicit include is the single opt-in
[ ] Docs contain copy-paste configuration and marker examples that run against the fixture
[x] Example proves a real embedded-host relationship, not parser output alone
[x] Upgrade is additive; no existing host scans Python without explicit scope
[x] No TypeScript or browser API surface is added
[x] Package proof runs non-interactively in CI
[x] No credit card, service account, or Python environment is required
[ ] Release notes/package notices capture accepted candidate or reproducible rejection
[x] Generated search is part of the proof
[x] Existing GitHub issue/community route remains available
```

### DX Consensus Table

| Topic | In-host DX review | Independent subagent | Resolution |
|---|---|---|---|
| Static safety | No Python execution must be stated and tested. | Same. | Accepted. |
| Repository scope | Broad scanning creates incidental-file noise. | Explicit include required. | Accepted as the one-setting opt-in. |
| Relationship discoverability | Link must be concrete in existing Docs. | Same, using the C# marker. | Accepted. |
| Native payload posture | Candidate needs a gated package story. | Suggested a zero-native product rule. | Retained user-approved measured gate; do not claim zero native before evidence. |

**Consensus:** 3/4 DX concerns confirmed; the zero-native assertion is intentionally not made because the spike is designed to measure and reject or accept a candidate, not assume the result.

### Cross-Phase Themes

- **Proof must be consumer-visible, not parser-visible.** CEO, engineering, and DX all reject a library-only benchmark: the evidence must be a packaged C#↔Python navigation/search outcome or a clear candidate rejection.
- **Static safety is a product promise.** Every phase preserves no Python execution; engineering enforces it in tests and DX makes it legible to adopters.
- **Explicit boundaries beat inferred behavior.** Fixture-local `__all__`, `IncludeGlobs`, literal ownership marker, canonical search provenance, and package gates all make the proof inspectable and reversible.
- **Native payload risk is a stop condition.** The plan does not amortize a failed dependency by broadening scope or silently substituting another parser.

## What I noticed about how you think

- You started with the broad pull of “a single tool to extract docs from everywhere,” then kept the immediate goal disciplined: prove the package reaches beyond a “direct dotnet ecosystem alone.”
- You separated the present from the future: “nothing [breaks]” today, while easier AI answers are a later hypothesis worth building evidence for rather than a promise this spike must make.
- “Python Polyglot embedded in broader dotnet codebase” gave the work a useful real-world shape without pretending the organization's final Python conventions are already settled.

## Consolidated Implementation Order

1. **Gate the candidate — T1 + T6.** Inspect the package archive with the bounded static gate. `TreeSitter.DotNet` `1.3.0` is rejected on size; do not run its packed package. A future candidate needs both a static pass and a separately approved operating-system-level sandbox before any runtime evidence is collected.
2. **Add bounded Python harvesting — T2 + T7.** Only after a pass, introduce Python options/source kind/path policy/registration/progress/health/diagnostics and static docstring/fixture-boundary tests.
3. **Make discovery visible — T3 + T8 + T9.** Add the source-only marker, internal candidate snapshot/linker, and language-qualified generated-search guard with complete JavaScript regressions.
4. **Prove the packaged outcome — T4 + T10.** Use the actual package consumer to verify C#↔Python navigation, search, anchors, diagnostics, and zero Python execution.
5. **Make it adoptable and measurable — T5 + T11 + T12 + T13.** Publish the golden path, error/upgrade reference, notices, and timed consumer journey evidence.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR | 3 shared concerns accepted; bounded ownership proof and package gate selected. |
| Codex Review | `/codex review` | Independent 2nd opinion | 1 | ISSUES SURFACED, RESOLVED | Challenged native bundle, general `__all__`, and parser-only value; user chose the bounded proof. |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 6 findings folded: child-process package smoke, full integration matrix, canonical search, internal linker. |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | SKIPPED | No product UI scope. |
| DX Review | `/plan-devex-review` | Developer experience gaps | 1 | CLEAR | 4.0/10 → 8.3/10 plan score; TTHW target is <=5 minutes for an existing Docs host. |

**CROSS-MODEL:** CEO voices agreed on the native-risk and false-negative-visibility concerns; engineering and DX voices independently reinforced executable package proof, explicit include scope, and consumer-visible links.

**VERDICT:** APPROVED — CEO + ENG + DX reviewed. Implementation remains gated on the candidate package evidence; no UI design review is required.

NO UNRESOLVED DECISIONS
